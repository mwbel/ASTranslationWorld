# 第23次 2026-07-04 13:07 本次任务重点整理

## 本次完成的任务重点

- 定位截图属于 AS译林本地副本，不属于 `tibetan-proofreading-app`。
- 验证 `两本书《洞见宇宙的奥秘》《窥探身心的机密》20231030docx.pdf` 的 PDF 文本层正确，开头为“编序”。
- 确认当前章节中旧页面是 `source_type: pdf_page`，`digital_text: null`，因此仍显示历史 OCR 结果。
- 修正 AS译林本地前端补丁：升级前端资产版本到 `file-router-v3`，升级 Service Worker 缓存名到 `as-yilin-v5-local-text-layer-router`。
- 优化 PDF.js 文本层抽取：按 PDF 文本坐标分行，而不是整页拼接成一行。
- 重建并启动本地 AS译林 web 容器。

## 下一步行动

- 前端刷新 `http://127.0.0.1:18088/`，必要时清理浏览器旧 Service Worker 缓存。
- 对可编辑 PDF 重新上传，检查新生成页是否为 `source_type: text` 且 `ocr_source: text_import`。
- 旧 OCR 页不会自动变成文本层页；如需替换，需要设计旧 PDF 页迁移或清理流程。

## 需要跟前端 app 同步的内容

- 新上传的可编辑 PDF 应走文本层直读并跳过 OCR。
- 旧 `pdf_page` 页面仍是历史 OCR 数据，界面上应避免把它误认为文本层结果。

## 需要跟管理后台同步的内容

- 暂无。

## 需要跟后端服务器同步的内容

- `upload-text` 接口已验证可用，返回 `page_id`、`segment_count`、`token_count`。
- 后端暂未提供单页删除或旧 PDF 页批量迁移入口。

## 需要跟数据库同步的内容

- 本轮接口探测生成了一个本地测试文本页，用于验证 `upload-text` 结果。
- 如果需要保持项目数据干净，下一步应提供页面级删除或测试数据清理工具。
