# Tibetan OCR Core ToDoList

更新时间：2026-06-28
当前负责人：Codex / 用户协作
当前模块：`tibetan-ocr-core`

## 使用规则

- 新任务直接追加到“待办”。
- 开始处理时，从“待办”移动到“进行中”。
- 完成后，从“进行中”移动到“已完成”。
- 如有阻塞，移动到“阻塞”并写清原因。
- 每次更新时，只保留必要信息，不堆积冗余过程记录。

## 当前目标

构建可独立发布的藏文 OCR 后端能力仓库，稳定提供本地 HTTP OCR 服务。

## 进行中

- [ ] 梳理 `bdrc_ocr_server.py` 的仓库边界
  目标：明确哪些代码属于 OCR 核心能力，哪些应该留给上层工作台。

## 待办

- [ ] 定义 OCR 仓库最小对外接口
  输出：明确 `POST /ocr`、`GET /health`、请求字段、返回字段。

- [ ] 梳理 OCR 输出结构
  输出：统一文本、行列表、bbox、页码、来源信息的数据格式。

- [ ] 明确输入边界
  输出：支持哪些文件类型、图片尺寸约束、PDF 页处理策略。

- [ ] 清理工作台耦合提示
  输出：把 OCR 核心仓里不该引用前端工作台目录名的地方全部替换掉。

- [ ] 补充 OCR 独立 README
  输出：让外部使用者只看 OCR 仓库就能启动和调用。

- [ ] 规划 OCR 独立发布目录
  输出：确认未来单独推送 GitHub 时保留哪些文件。

## 阻塞

- [ ] 暂无

## 已完成

- [x] 将 OCR、翻译服务、校对工作台拆成三类目录
- [x] 保留 OCR 后端独立开发入口：`tibetan-ocr-core/bdrc_ocr_server.py`

## 下一步建议

1. 先梳理 `bdrc_ocr_server.py` 的接口边界。
2. 再统一 OCR 返回结构。
3. 最后整理成可单独发布的 GitHub 仓库形态。
