# 第96次任务重点整理：模型名称精确化

## 本次完成的任务重点

- 将 AS译林聚合适配器模型主 ID 改为可直接识别版本、参数规模和量化方式的精确名称。
- 保留旧模型 ID 作为 alias，兼容已经保存的调用配置。
- 同步运行配置、示例配置、说明文档和测试断言。
- 将 ModelAggregatorService 和 AS译林聚合适配器的服务版本同步更新为 `0.2.0`；接口路径保持不变。
- 将模型性能画像结构版本更新为 `profileVersion: 2`，注册表修订号为 `2026-07-13`。
- 已重启聚合器并重新构建适配器容器；`/v1/models` 只展示新的精确主 ID。
- 已实际验证 `agg-gemini-2.5-flash` 与旧名称 `agg-gemini-25-flash` 均可调用并返回 `OK`。

## 后续使用

- 在 AS译林本地副本中刷新模型列表，后续新配置统一选择精确主 ID。
- 已保存的旧模型 ID 无需立即迁移，适配器会继续通过 alias 兼容。

## 需要跟前端交流的内容

- 前端应显示适配器返回的新主 ID 和现有 `display_name`，不再自行省略模型版本中的小数点。

## 各模块同步事项

- 前端 app：本次不改；后续刷新模型列表即可。
- 管理后台：本次不改；若缓存了旧 ID，可继续通过 alias 使用。
- 后端服务器：服务版本为 `0.2.0`，使用新的精确主 ID，对旧 ID 保持兼容；API 路径不变。
- 数据库：本次不改，不迁移已有模型记录。
