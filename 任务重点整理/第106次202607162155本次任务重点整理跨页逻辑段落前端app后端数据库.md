# 第106次本次任务重点整理：跨页逻辑段落

## 本次完成的任务重点

- 为 AS译林本地副本增加独立跨页关联服务和可重复执行的数据表迁移。
- 下一页首段可显示“跨页续段候选”，只有人工确认后才建立逻辑关联。
- 两页原始段落、source token 和既有译文不删除、不迁移。
- 翻译已确认的主段时，临时组合上一页末段与下一页首段，完成后将译文归回主段并清理临时段落。
- 已确认关系支持撤销；刷新后状态仍持久化。

## 下一步行动

- 用更多文档样本校准候选规则，重点排除标题、列表、引文和页眉页脚。
- 观察实际模型翻译后的 source mapping 是否需要增加跨页来源标注。

## 需要跟前端交流的内容

- 候选不会自动合并；确认和撤销都位于下一页首段卡片顶部。
- 已确认后，AI 翻译会自动使用两页合并原文。

## 模块同步

- 前端 app：增加候选、确认状态和撤销按钮。
- 管理后台：本轮无需改动。
- 后端服务器：增加仅本地使用的跨页关联与翻译代理服务。
- 数据库：新增 `local_cross_page_links`，不修改原 segments/source_tokens/translation_proposals 结构。
