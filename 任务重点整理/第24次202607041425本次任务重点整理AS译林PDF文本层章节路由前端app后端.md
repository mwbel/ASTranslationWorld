# 第24次 2026-07-04 14:25 本次任务重点整理

## 本次完成的任务重点

- 定位右栏错字不是 PDF 文本层错误，而是旧 OCR 页仍在当前章节前面。
- 已确认原 PDF `两本书《洞见宇宙的奥秘》《窥探身心的机密》20231030docx.pdf` 文本层可正确抽取。
- 已确认 AS译林数据库中已有正确 `text_import` 记录，但此前被追加到旧 OCR 章节末尾。
- 修正 AS译林本地前端文件路由：项目级上传直读文本时按文件名新建章节，不再复用最后一个旧章节。
- 已重建并重启 `sutra-local-web`。
- 已非破坏性新建章节 `两本书《洞见宇宙的奥秘》《窥探身心的机密》文本层直读`，导入正确文本。
- 新章节 9235 个 token 中，用户指出的 OCR 错字命中数为 0。

## 下一步行动

- 在 AS译林前端刷新页面。
- 打开项目 `洞见`。
- 切换到新章节 `两本书《洞见宇宙的奥秘》《窥探身心的机密》文本层直读`。
- 从该章节继续校对和 AI 翻译。

## 需要跟前端 app 同步的内容

- 项目级 PDF 上传如果可直读文本层，应创建独立文本章节。
- 不应把直读文本追加到已有 OCR/PDF 页面章节末尾。
- 浏览器如仍显示旧结果，需要强制刷新以加载新的文件路由脚本。

## 需要跟管理后台同步的内容

- 暂无。

## 需要跟后端服务器同步的内容

- 暂无后端业务代码变更。
- 本次通过现有 `/api/projects/:id/chapters` 和 `/api/chapters/:id/upload-text` 完成非破坏性导入。

## 需要跟数据库同步的内容

- 已新增一个文本层直读章节和对应文本页。
- 未删除旧 OCR/PDF 页面，避免破坏已有数据。
