# 第90次 202607112210 本次任务重点整理：编辑区隐藏内置角色

## 本次完成的任务重点

1. 将“隐藏内置角色”逻辑从角色管理弹窗扩展到右栏新译文编辑区的角色按钮。
2. 当前规则为：
   - 角色管理中点击“隐藏内置角色”后
   - 右栏 `.translation-editor .persona-selector` 中对应的旧内置角色副本也会一起隐藏
3. 为了在弹窗关闭后仍能判断哪些名称属于 `内置`、哪些属于 `我的`，前端会把角色名称缓存到浏览器 `localStorage`。
4. 已重建并强制重建本地 `sutra-local-web`，运行态资源中已包含这条编辑区隐藏逻辑。

## 下一步行动

1. 用户刷新 `http://127.0.0.1:18088/`。
2. 打开角色管理，确认“隐藏内置角色”仍可用。
3. 再进入右栏新译文编辑区，确认重复角色按钮被精简。

## 需要跟前端交流的内容

- 当前实现仍是本地前端补丁，不是原应用源码内的正式角色系统重构。
- 如果后续要做得更稳，建议把“内置/我的”标识直接带到编辑区按钮数据里，而不是在 DOM 层推断。

## 前端app、管理后台、后端服务器、数据库各自下一步

- 前端 app：
  - 观察这种“同步隐藏内置角色”的策略是否满足使用体验。
  - 若还需要进一步精简，可继续做“只显示我常用的角色”。
- 管理后台：
  - 当前无需改动。
- 后端服务器：
  - 当前无需改动。
- 数据库：
  - 当前无需改动。
