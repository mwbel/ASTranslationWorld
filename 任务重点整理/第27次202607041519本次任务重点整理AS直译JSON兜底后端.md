# 第27次 2026-07-04 15:19 本次任务重点整理：AS直译JSON兜底 后端

## 本次完成的任务重点

- 按 AS译林 LLM 调用链路排查直译失败：Docker 与 AS译林前端可用，失败先出现在上游 `gemini:gemini-2.5-flash` 配额耗尽，随后收敛到本地模型直译 JSON 输出不稳定。
- 将 `as-yilin-model-adapter/config.yaml` 的默认上游从 Gemini 切到已验证可用的 `local:qwen2.5:14b`，同时保留 `gemini-3-flash-preview` 等旧别名。
- 在 `as-yilin-model-adapter/adapter_server/providers.py` 增加 literal 直译请求识别与 JSON 兜底：只对包含 `token_id/literal` 的直译结构化请求生效，强制输出合法数组、去重 token id、只保留请求中存在的 token id。
- 重建并重启 `as-yilin-model-adapter` adapter 容器。
- 将本次已失败的 `generate_literals_segment` job 重置为 `queued`，让它们用新的适配器重新跑。

## 下一步行动

- 等待当前直译队列继续消费；当前观察到重置后的新任务持续 `done`，未再出现新失败。
- 如果前端仍显示旧失败状态，刷新页面或等待队列状态同步后再看；必要时再查 `jobs` 表中当前页面对应的失败 job。

## 需要跟前端 app 交流的内容

- 前端失败提示应区分：模型网关 502、模型输出 JSON 不合规、数据库入库失败、历史 failed job 未重试。
- 顶部“运行中 / 失败”状态最好显示最新失败原因摘要，避免只看到计数。

## 需要跟管理后台同步的内容

- 管理后台如展示模型列表，当前默认模型应显示为 `ModelAggregator Local Qwen2.5 14B`。
- 后续可增加“重试失败直译任务”的管理动作，避免直接手工改 `jobs` 表。

## 需要跟后端服务器同步的内容

- AS译林 server 当前直译格式要求为 `[{ "token_id": "...", "literal": "...", "alt": [], "note": "" }]`。
- 本次没有修改 AS译林 server 二进制；结构化保护放在模型适配器层。
- 若后续拿到 AS译林后端源码，应在直译服务内部增加输出校验与逐条容错，避免整个 job 因单个无效 token id 失败。

## 需要跟数据库同步的内容

- 本次仅重置最近两小时内本次直译任务的 failed job：`status='queued'`、`attempts=0`、清空 `error/started_at/finished_at`。
- 未删除 `literal_translations`、`source_tokens`、`segments` 或用户正文数据。
