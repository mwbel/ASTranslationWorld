# 第87次 2026-07-11 17:14 本次任务重点整理：入口分流修复

## 本次完成的任务重点

- 修复首页两个“新建项目”入口都进入 OCR 校对页面的问题。
- OCR 入口保持进入 OCR 校对工作台。
- 藏译中入口进入“藏译中工作台”，显示原文与翻译结果两栏。
- 进入藏译中时清理 OCR 校对合并视图残留状态，避免继承 `proofread-merged-view`。

## 下一步行动

- 前端 app：后续可继续把藏译中工作台做成更独立的任务页，例如只保留原文、待翻译文本、译文编辑和导出。
- 管理后台：暂无同步事项。
- 后端服务器：暂无接口变更。
- 数据库：暂无数据库变更。

## 需要同步的内容

- 前端 app：入口分流现在依赖 `activeWorkflow`、`translation-workflow` 和 `translation-enabled` 类。
- 管理后台：无需同步。
- 后端服务器：无需同步。
- 数据库：无需同步。
