# 第41次 2026-07-04 23:43 本次任务重点整理：AS页面性能

## 本次完成

- 定位 AS译林本地副本页面卡顿主要风险点在前端注入的原文 block 同步逻辑。
- 原逻辑对 `document.body` 做全量 `MutationObserver`，并监听 body 尺寸变化，容易在段落较多时造成主线程频繁测量和写样式。
- 已将同步逻辑升级为 `v9-perf`：
  - 只绑定 `.text-source-viewer__content` 与 `.segments-list` 两个相关容器。
  - Mutation / Resize 触发后做 120ms 节流。
  - 只在 block 高度变化时写入 CSS 变量。
  - 静态资源版本升级为 `index-ex7lBOJf-file-router-v9.js`。
- 已重建并重启本地 `sutra-local-web` 容器，`18088` 已加载 v9 资源。

## 下一步行动

- 在用户当前浏览器中刷新 `http://127.0.0.1:18088/?_sutra_local_v=9-perf`，确认无“页面无响应”弹窗。
- 若仍卡顿，下一步检查具体章节的段落数量、接口 503/重试频率、以及是否存在旧 service worker 缓存。

## 前端 app

- 已修改 `sutra-image-package-20260611-2159-server-runtime-amd64/Dockerfile.web-login-patch`。
- 需要前端继续关注：原文 block 同步只做视觉高度对齐，不应承担数据同步或重排控制职责。

## 管理后台

- 无需同步。没有改项目/成员/权限管理逻辑。

## 后端服务器

- 无需同步。没有改 AS译林后端接口、LLM 适配器或模型聚合服务。

## 数据库

- 无需同步。没有改页面、段落、译文或用户表结构。
