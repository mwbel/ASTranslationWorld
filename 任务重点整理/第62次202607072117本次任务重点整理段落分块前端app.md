# 第62次 2026-07-07 21:17 本次任务重点整理：段落分块

## 本次完成的任务重点

- 将本地 AS译林文本型 PDF 直读链路从视觉行级 block 改为段落级 block。
- 在本地 web 补丁中新增 `normalizeDirectTextForSegmentation`：PDF 文本层抽取后先合并连续视觉行，再以自然段作为后端 `split_mode=line` 的输入单位。
- 保留 Markdown / TXT 原文本身的换行结构，不强行套用 PDF 行合并规则。
- 将本地静态资源版本提升到 `file-router-v12`，避免旧缓存继续加载行级分段逻辑。
- 用 `洞见中译英` 第 2 页现有 `digital_text` 模拟验证：23 个视觉行可合并为 8 个段落 block。

## 下一步行动

- 用户刷新 `http://127.0.0.1:18088/` 后，新上传或重新直读 PDF 会按段落生成 block。
- 当前第 2 页已有译文 proposal，未自动重分段覆盖；若需要重排当前页，需明确允许覆盖当前页段落/译文关系后再执行。

## 前端 app

- 本地前端文件路由已加入 PDF 段落合并逻辑。
- UI 仍显示原有分段卡片；改变的是分段输入单位。

## 管理后台

- 暂无管理后台动作。

## 后端服务器

- AS 后端代码未修改；继续使用现有 `/upload-text` 与 `/resegment` 接口。

## 数据库

- 本次未直接修改段落数据。
- 检查到当前第 2 页已有 1 条译文 proposal，因此未自动覆盖当前页分段。
