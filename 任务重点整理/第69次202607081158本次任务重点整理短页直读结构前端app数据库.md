# 第69次 202607081158 本次任务重点整理：短页直读结构

## 本次完成的任务重点

- 定位 AS译林本地副本“全部直读后右栏无结构”的链路：PDF 文本层抽取成功页已进入 `/resegment`，但短图题页因单页阈值过高被跳过。
- 将本地 Web 补丁升级为 `v18-short-pdf-page-direct-read`，保留整本文档 40 字有效文本门槛，同时把单页直读降为短文本可用门槛。
- 用正式 `/api/pages/{id}/resegment` 接口补齐当前章节短文本页结构：`done_pages` 从 536/564 提升到 562/564，剩余 2 页为空白页。

## 下一步行动

- 前端 app：刷新 `http://127.0.0.1:18088/` 后检查 P8、P90、P108 等短图题页，确认右栏出现分段结构。
- 管理后台：暂无同步事项。
- 后端服务器：后续如要彻底稳定，应在后端文本层抽取/分页阶段持久化“短文本页可直读、空白页可标记跳过”的状态。
- 数据库：当前只通过后端正式接口写入 segments/source_tokens；不直接改表结构。

## 需要同步的内容

- 前端 app 需要区分“空白页”和“短文本页”：短文本页应显示结构，空白页不应显示为 OCR 错误。
- 后端服务器后续可增加空白页状态，避免侧栏长期显示普通 pending。
