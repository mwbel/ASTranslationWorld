# 第88次 202607111612 本次任务重点整理：全模型对比

## 本次完成的任务重点

1. 在 `as-yilin-model-adapter` 中新增“全模型对比”虚拟模型：
   - 模型 ID：`agg-compare-all`
   - 显示名：`ModelAggregator 全模型对比`
2. 该虚拟模型被选中时，不再只调用单个上游，而是并行调用当前用户可用的全部真实模型，并把结果合并为一份对比文本返回。
3. 当前纳入对比的可用模型包括：
   - `agg-local-qwen25`
   - `agg-local-qwen36`
   - `agg-local-gemma2`
   - `agg-local-qwen36-q4`
   - `agg-gemini-25-flash`
4. 适配器层调用成功后，返回文本按“模型名 + 上游模型 + 用时 + 译文/错误”组织，前端无需新增页面结构即可复用现有译文展示区。
5. 已重建并重启 `as-yilin-model-adapter` 的 Docker 容器，运行态 `/models` 已返回 6 个模型，其中包含 `agg-compare-all`。
6. 已实测 `agg-compare-all` 经 `18081` 返回“全模型翻译对比”文本，功能已进入运行态。

## 下一步行动

1. 在本地 `AS译林` 前端角色管理中点击“重新拉模型”，让界面刷新出 `ModelAggregator 全模型对比`。
2. 为需要对比的角色单独绑定该模型，而不是替换所有正常单模型角色。
3. 视实际使用反馈，再决定是否需要做第二阶段优化：
   - 单独的“对比视图”
   - 更紧凑的卡片化展示
   - 一键采纳某个模型结果

## 需要跟前端交流的内容

- 当前版本复用现有译文编辑区显示对比结果，因此输出是“长文本对比稿”，不是并排卡片界面。
- 如果前端后续希望更易读，建议下一阶段把 `provider_response.comparison` 渲染成逐模型卡片，而不是只展示合并后的纯文本。

## 前端app、管理后台、后端服务器、数据库各自下一步

- 前端 app：
  - 角色管理里执行“重新拉模型”。
  - 验证选择 `ModelAggregator 全模型对比` 后，译文区出现多模型结果对比文本。
- 管理后台：
  - 当前无需改动。
- 后端服务器：
  - 保持 `agg-compare-all` 逻辑只在适配器层实现，避免把多模型并发逻辑散落到 AS 后端。
  - 后续如新增模型，只要它已在适配器中可用，就会自动被纳入对比候选。
- 数据库：
  - 无需结构变更。
  - 继续沿用当前 `gv-reseller` / adapter endpoint 配置。
