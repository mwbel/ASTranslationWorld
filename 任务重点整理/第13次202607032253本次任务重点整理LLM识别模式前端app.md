# 第13次 2026-07-03 22:53 本次任务重点整理：LLM识别模式 前端app

## 本次完成的任务重点

- 增加 OCR 识别引擎下拉：智能（BDRC + LLM）、仅本地 BDRC、仅 AI Vision。
- 增加 AI Vision OCR 接口输入，默认 `http://127.0.0.1:18092/ocr`。
- 智能模式会先调用本地 BDRC，再把页图、BDRC 初稿和高危字符簇提交给 AI Vision 接口校正。
- 文字可编辑 PDF 仍优先直接提取文本，不调用 OCR。

## 下一步行动

- 后端需要实现 AI Vision OCR HTTP 服务，兼容返回纯文本或 `{ text, lines }`。
- 前端刷新后测试三种模式的状态提示和失败提示。

## 前端 app

- 下一步：根据实际 AI Vision 服务返回格式微调解析。

## 管理后台

- 下一步：暂无。

## 后端服务器

- 下一步：新增 `18092/ocr` AI Vision OCR 适配服务。

## 数据库

- 下一步：暂无。
