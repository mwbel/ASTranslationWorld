# 第103次任务重点整理：Zeabur 藏文 OCR 部署

## 本次完成的任务重点

- 增加 Zeabur 单服务部署入口，统一提供工作台静态页面、OSS 建书与状态接口、BDRC 上游代理、AI Vision OCR 和藏译中接口。
- 云端上传时把待校对书籍写入同一个私有阿里云 OSS Bucket，并按 `book_id` 隔离原书、状态和导出。
- 云端前端自动使用同域 API；本地端口和 localStorage 调试链路保持不变。
- 明确本地 macOS BDRC 模型不能原样进入 Zeabur Linux 容器，必须配置独立 BDRC 上游。

## 下一步行动

- 在阿里云控制台新建私有 Bucket 与最小权限 RAM 凭证。
- 在 Zeabur 绑定 GitHub 服务、填写环境变量并部署。
- 为正式协作补充书库列表、用户身份/owner 权限，以及仅凭 `book_id` 恢复源文件和工作区。

## 需要跟前端交流的内容

- 云端地址会自动切换到 `/api/ocr`、`/api/ai-ocr`、`/api/translate`。
- 当前状态已写 OSS，但跨设备书库恢复入口仍待补充。

## 需要同步的内容

- 前端 app：保留本地缓存，同时同步云端状态。
- 管理后台：后续需要书籍列表、分配人与状态筛选。
- 后端服务器：需配置真实 BDRC 上游、模型聚合器和健康检查。
- 数据库：本阶段元数据与状态存 OSS JSON；多人正式使用前应迁移到 Postgres，并保留 OSS 只存文件与导出。
