# 第29次 2026-07-04 15:32 本次任务重点整理：Gemini Key 轮值

## 本次完成的任务重点

- 确认 AS译林直译链路中的 Gemini 429 发生在 `ModelAggregatorService` 上游 provider 调用环节，不是 AS 前端、AS 后端、OCR 或 PDF 文本抽取问题。
- 确认聚合器原本已有基础 key 轮值：每次请求从不同 key 开始，并在 429/5xx 等错误后继续试下一个 key。
- 在 `ModelAggregatorService/model_aggregator/services/model_aggregator.py` 增强 Gemini key 池：
  - 429、401/403、5xx、网络异常后标记对应 key 冷却。
  - 后续请求跳过仍在冷却中的 key。
  - 尝试日志中的 `key#N` 改为配置 key 池中的真实序号，而不是本轮尝试序号。
  - 当所有 key 都处于冷却中时返回明确冷却提示。
- 已重启本地 `ModelAggregatorService`，服务地址仍为 `http://127.0.0.1:8890`。
- 已验证 `gemini:gemini-2.5-flash` 真实调用返回 `OK`。

## 下一步行动

- 如 AS译林继续使用 Gemini，可把 AS 适配器上游从当前本地 `local:qwen2.5:14b` 切回 `gemini:gemini-2.5-flash` 后测试直译。
- 如果 Gemini 仍遇到全 key 429，需要增加更长冷却窗口或配置备用模型 fallback，例如自动切到 `local:qwen2.5:14b`。

## 需要跟前端交流的内容

- 前端无需展示 API key，也无需保存 Gemini key。
- 如果后端返回“所有 Gemini key 正在冷却”，前端可直接展示该错误，不要泛化成普通 502。

## 需要跟前端 app 同步的内容

- AS译林前端无改动。
- 藏文 OCR 工作台前端无改动。

## 需要跟管理后台同步的内容

- 暂无管理后台改动。
- 后续如要管理 key 池状态，可新增只显示数量和冷却状态的只读接口，不暴露真实 key。

## 需要跟后端服务器同步的内容

- `ModelAggregatorService` 的 Gemini provider 调用逻辑已增强。
- `/api/aggregate/chat`、`/api/aggregate/models`、`/api/aggregate/image-to-markdown` 的 Gemini 分支都会应用 key 冷却。
- API 路径和响应字段保持兼容。

## 需要跟数据库同步的内容

- 无数据库改动。
