# 第31次 202607041634 本次任务重点整理 AIVision行数模型 前端app后端服务器

## 本次完成的任务重点

- 确认当前 AI Vision OCR 运行模型为 `gemini:gemini-2.5-flash`，通过 `ModelAggregatorService` 转发。
- 修正前端 AI Vision prompt，明确要求按 BDRC 行数逐行完整返回，不能只输出前几行。
- 修正 AI OCR 后端默认 prompt，保证非前端客户端调用时也要求整页完整逐行输出。
- AI 栏状态改为显示模型与返回行数，便于判断是否只返回了部分内容。

## 下一步行动

- 重新识别当前 PDF 第 1 页，检查 AI Vision 是否从 4 行提升到接近 BDRC 行数。
- 若仍缺行，下一步应改为“分块/逐行裁切后调用 AI Vision”，而不是整页一次性让 Gemini 识别。

## 前端 app

- AI Vision 栏继续显示独立结果，并显示模型与 `返回行数 / BDRC 行数`。
- 智能识别后若 AI 返回行数不足，应在状态栏明确提示部分返回。

## 管理后台

- 本轮无管理后台改动。

## 后端服务器

- AI OCR 服务默认 prompt 已加强完整逐行输出约束。
- 后续如果模型仍只返回部分行，后端需要增加按行裁切或分页分块识别接口。

## 数据库

- 本轮无数据库改动。
