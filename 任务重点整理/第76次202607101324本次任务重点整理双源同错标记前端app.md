# 第76次 2026-07-10 13:24 本次任务重点整理：双源同错标记 前端app

## 本次完成的任务重点

- 在藏文 OCR 校对视图中新增“双源同错”人工标记能力。
- 支持在 BDRC 或 AI Vision 的可编辑 block 中选中文字后标记为同错。
- 如果另一侧存在相同字符，自动同步标记；否则保留当前侧标记并提示用户。
- 新增清除同错标记能力：有选区时清除相交标记，无选区时清除当前 block 全部同错标记。

## 下一步行动

- 前端 app：用样本 PDF 手动测试选区标记、清除、切页缓存恢复、保存修改。
- 管理后台：暂无同步事项。
- 后端服务器：暂无同步事项；本次不改 OCR 后端。
- 数据库：暂无同步事项；同错标记当前随浏览器本地 OCR 结果缓存保存。

## 需要同步的内容

- 前端 app：同错标记是人工校对层数据，不是 BDRC 或 AI Vision 自动判断结果。
- 后端服务器：如果后续要多人协作或长期保存，需要将 `compare.sharedErrors` 持久化到服务端。
