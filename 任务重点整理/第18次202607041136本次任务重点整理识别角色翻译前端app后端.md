# 第18次 2026-07-04 11:36 本次任务重点整理：识别角色翻译 前端app后端

## 本次完成的任务重点

- 复核前端已提供三种识别模式：智能（BDRC + LLM）、仅本地 BDRC、仅 AI Vision。
- 复核并保留“宗萨参考英译中”内置翻译角色，角色提示词通过翻译请求传入后端。
- 将内置翻译角色默认模型切换为已实测可用的 `gemini:gemini-2.5-flash`，避免默认走当前超时的本地模型。
- 启动并验证本地前端、BDRC OCR、AI Vision OCR、翻译服务与模型聚合器。
- 使用临时 mock 上游验证 AI Vision OCR 的 OpenAI-compatible 图片请求和响应解析。
- 使用真实模型聚合器验证翻译接口返回译文，并确认返回中包含 `dzongsar-reference-en-zh` 角色信息。
- 将 AI Vision OCR 默认上游改为 ModelAggregatorService 的 `/api/aggregate/image-to-markdown`，并保留 OpenAI-compatible 直连模式。
- 修复智能识别模式：当 BDRC 返回 `No valid lines after filtering` 等无有效行错误时，自动 fallback 到 AI Vision，而不是整次识别失败。

## 下一步行动

- 对真实扫描 PDF 做一次完整手动测试：上传、切换三种识别模式、识别当前页、翻译、编辑、导出。
- 如果继续使用本地模型，优先修复 `local:qwen*` 和 `local:llama3` 在模型聚合器中的超时问题。

## 需要跟前端交流的内容

- 识别引擎下拉菜单已有三种模式，智能模式会先取 BDRC 结果，再尝试调用 AI Vision 校正。
- 如果智能模式下 BDRC 没有返回有效行，前端会自动改用 AI Vision 作为 fallback。
- 翻译角色下拉中已有“宗萨参考英译中”，默认模型显示为 `gemini:gemini-2.5-flash`。
- 真实 AI Vision 识别现在默认依赖 ModelAggregatorService；如果聚合器未开，前端应显示 AI OCR 调用失败，而不是误判为 BDRC 问题。

## 前端 app

- 下一步：在浏览器中上传样例扫描 PDF，逐项点击三种识别模式并观察状态提示。

## 管理后台

- 下一步：暂无。

## 后端服务器

- 下一步：保持 ModelAggregatorService 的 Gemini 图片识别额度可用；如需离线模式，再配置 `AI_VISION_PROVIDER=openai-compatible` 接本地视觉模型。

## 数据库

- 下一步：暂无；本地工作台当前仍使用浏览器本地缓存保存 OCR 与翻译结果。
