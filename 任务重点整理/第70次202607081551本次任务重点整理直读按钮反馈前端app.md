# 第70次 2026-07-08 15:51 本次任务重点整理：直读按钮反馈

## 本次完成的任务重点

- 修复 AS译林本地副本中“重新直读 / 全部直读”点击后缺少可见反馈的问题。
- 为 PDF 文本层直读加入右上角状态提示：开始、单页完成、批量完成、无有效文本层、请求失败。
- 将直读确认从原生确认弹窗改为本地直读提示，避免用户误以为仍在跑 OCR / LLM Vision。
- “全部直读”只处理待直读页面，不再重复处理已完成页面，减少页面卡顿和无响应感。

## 下一步行动

- 前端 app：刷新 `http://127.0.0.1:18088/` 后，重新测试“重新直读”和“全部直读”；若仍无右栏结构，下一步检查当前页是否已有 segments 或是否是空白页。
- 管理后台：暂无同步事项。
- 后端服务器：本次未改 AS译林后端 API；如后续仍失败，再检查 `/api/pages/{id}/resegment` 返回体。
- 数据库：本次不直接改库；若历史页面状态与 segments 不一致，再用 API 重新分段，避免手动写库。

## 需要同步的内容

- 前端 app：直读按钮现在会显示本地状态提示；用户看到“无有效文本层”时表示该页不是可直读文本页或页面为空，不会自动进入 OCR。
- 管理后台：无。
- 后端服务器：无接口变更。
- 数据库：无 schema 变更。
