# 第5次任务重点整理

## 本次完成的任务重点

- 已确认 `http://127.0.0.1:18088/` 当前翻译链路不是 `tibetan-proofreading-app` 三栏页，而是 `AS译林 -> BYO key -> as-yilin-model-adapter`。
- 已通过 `probe_server` 抓到实际协议：
  - `POST /api/user/login`
  - `GET /v1/models`
  - `POST /v1/chat/completions`
- 已把 `as-yilin-model-adapter/config.yaml` 的默认上游改成 Hugging Face OpenAI-compatible Router。
- 已把默认模型改成 `Qwen/Qwen2.5-7B-Instruct`，用于先完成中译英/直译类 MVP。
- 已新增模型别名映射，让 AS译林 当前仍在请求的 `gemini-3-flash-preview` 自动落到新的开源模型。
- 已补 `docker-compose.yml` 的 `HF_TOKEN` 透传和 `.env.example` 说明。

## 下一步行动

- 前端 app：
  - 在 `BYO key` 中继续使用 `gateway_endpoint=http://host.docker.internal:18081`。
  - 点击 `测试：拉取我的模型` 后，再回到工作台点单段 `✦ AI 生成逐词直译` 做页面验证。
- 管理后台：
  - 暂无代码修改。
  - 如角色仍绑定旧模型名，可后续在角色配置里显式改为新开源模型展示名。
- 后端：
  - 在适配器运行环境中注入 `HF_TOKEN`。
  - 重建 `adapter` 容器，使新配置和别名映射生效。
- 数据库：
  - 本次未改数据库结构。
  - 后续只需观察新译文写回是否正常，无需先改表。

## 需要跟前端交流的内容

- 当前页面真正吃的是 `BYO key` 网关协议，不是本仓库里的静态三栏页翻译接口。
- 直译按钮点下后，请求体是标准 `messages` + `model` + `max_tokens`，不需要前端改协议。
- 页面当前请求的模型名仍可能是旧值 `gemini-3-flash-preview`；本次后端已用别名兼容，前端暂时不用阻塞在角色改名上。

## 需要同步的内容

- 前端 app：
  - 继续沿用 `/v1/models` 与 `/v1/chat/completions` 协议。
- 管理后台：
  - 暂无同步项。
- 后端服务器：
  - 需要保证 `HF_TOKEN` 已注入适配器容器。
  - 需要重建 `as-yilin-model-adapter` 的 `adapter` 服务。
- 数据库：
  - 暂无同步项，本次不涉及表结构和迁移。
