# 第11次 2026-07-03 22:35 本次任务重点整理：同步框漂移 前端app

## 本次完成的任务重点

- 修复左栏整行同步框在滚动容器、居中画布或浏览器缩放下的坐标漂移。
- 同步框定位改为基于 `getBoundingClientRect()` 加滚动偏移计算。
- 不修改 OCR 后端、翻译后端、缓存结构和中栏高危标记。

## 下一步行动

- 刷新页面后点击多行 OCR 结果，观察左栏同步框是否仍漂移。
- 若仍有偏差，下一步检查 OCR 后端返回的 line bbox 是否与实际页面图像坐标一致。

## 前端 app

- 下一步：必要时增加 bbox 调试开关，显示原始 x/y/width/height。

## 管理后台

- 下一步：暂无。

## 后端服务器

- 下一步：确认 BDRC bbox 坐标来源是否和前端渲染图一致。

## 数据库

- 下一步：暂无。
