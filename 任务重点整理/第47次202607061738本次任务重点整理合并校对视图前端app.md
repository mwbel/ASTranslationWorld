# 第47次 2026-07-06 17:38 本次任务重点整理

## 本次完成的任务重点

- 在藏文 OCR 工作台增加 `校对` Tab。
- `校对` 视图保留左侧原文页，右侧合并 BDRC 与 AI Vision 两栏。
- 合并区按 block 展示：原文 block 预览、BDRC 识别、AI Vision 识别。
- BDRC 与 AI Vision 文本均可编辑，并保留高危字符与差异预览。
- 每个 block 可选择采用 BDRC 或 AI Vision，并通过 `保存修改` 写回当前页 OCR 结果。

## 下一步行动

- 用当前藏文 PDF 完整跑一页 OCR，检查 block 顺序、原文裁切预览和保存后的导出结果。
- 如果原文 block 裁切仍不清晰，下一步只调整裁切 padding 与显示高度，不改 OCR 数据结构。

## 前端 app

- 下一步重点验证 `校对 / 对照 / 逐行 / 文本` 四个视图切换是否互不影响。
- 验证点击左栏同步框、中栏 block、AI block 后的定位与滚动是否一致。

## 管理后台

- 暂无同步事项。

## 后端服务器

- 暂未修改 OCR 或 AI Vision 服务接口。
- 后续如需要提高 block 坐标准确度，再评估后端是否返回更稳定的 bbox。

## 数据库

- 暂无数据库改动。
