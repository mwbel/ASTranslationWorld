# 第8次 2026-07-02 21:54 本次任务重点整理：AS译林LLM规则

## 本次完成的任务重点

- 参考 `ocr-review-workbench/AGENTS.md` 的项目边界、链路约束和发布规则结构，更新本项目 `AGENTS.md`。
- 将当前最高优先级明确为跑通本地 AS译林通过 LLM API key / BYO key / 聚合适配器调用模型。
- 新增 AS译林 LLM API key 跑通链路 debug 规则。
- 新增文本型文档直读规则：Markdown、TXT、DOCX、可编辑 PDF 优先直接抽取文本，扫描件才进入 OCR。
- 保留“只改错误模块，不修改其他模块”的约束。

## 下一步行动

- 按新 AGENTS 规则继续排查 AS译林本地副本的 LLM API key 链路。
- 优先确认 Docker 容器、自动登录、BYO key、适配器、外部模型 API、响应格式这几个环节。

## 需要跟前端交流的内容

- 当前仅更新协作规则，没有修改前端。
- 后续若前端 BYO key 配置或错误提示不清晰，再按链路证据单独调整前端模块。

## 前端 app 下一步

- 暂无代码同步事项。

## 管理后台下一步

- 暂无同步事项。

## 后端服务器下一步

- 后续重点验证 AS译林后端能否通过 `host.docker.internal` 访问本地 LLM 适配器。
- 若文档上传链路继续默认 OCR，需要新增文本型文档直读分支。

## 数据库下一步

- 暂无同步事项。
