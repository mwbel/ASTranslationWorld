# 第18次 2026-07-03 21:37 本次任务重点整理：PDF文件路由器

## 本次完成的任务重点

- 在本地 AS译林 web 补丁中加入文件路由器：上传 PDF / Markdown / TXT 时，先尝试从浏览器端直接提取文本。
- 文本层有效时改走 `/api/chapters/{chapterId}/upload-text`，跳过 OCR；文本不足或失败时回退原上传链路。
- 为本地前端加入 `data-sutra-file-router="ready"` 运行时标记，便于确认浏览器实际执行补丁。
- 增加 PDF.js 静态资源到本地 web 镜像，并把前端入口切到 `index-ex7lBOJf-file-router-v2.js`，避免浏览器旧缓存。
- 已验证本地 AS译林健康检查、前端入口、PDF.js 静态资源、浏览器运行时标记。
- 已用测试文本层 PDF 验证后端文本页链路：新增页 `source_type=text`，`ocr_status=done`，没有进入 OCR 待识别状态。

## 下一步行动

- 用用户真实可编辑 PDF 在前端上传控件走一次手动冒烟测试。
- 若用户仍看到 OCR 状态，优先检查是否是浏览器缓存、文件文本层抽取不足、或上传入口未走 AS 前端文件路由器。
- 后续再补 DOCX / Word 的前端直读体验；当前 Word 仍依赖后端已有 Pandoc 文档解析链路。

## 需要跟前端 app 交流的内容

- PDF 上传进度文案已改为“检测 PDF 文本层”，可直读时不应显示等待 OCR 的导入结果。
- 文件路由器是前端 `fetch` 包装层：只对页面上传生效，直接 `curl /upload` 不会触发。
- 运行时可检查 DOM：`data-sutra-file-router="ready"`。

## 需要跟管理后台同步的内容

- 本次不涉及管理后台。

## 需要跟后端服务器同步的内容

- 后端无需改动；文件路由器使用现有 `/upload-text` 接口。
- 验证结果显示 `/upload-text` 可以生成文本页，状态为 `ocr_status=done`。

## 需要跟数据库同步的内容

- 本次没有改数据库结构。
- 本地测试向“洞见 / 未分章”新增了一个文本测试页，用于验证 PDF 文本直读落库链路。
