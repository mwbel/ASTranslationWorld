# 第119次本次任务重点整理：PDF 段落精确同步

## 本次完成的任务重点

- 移除按 block 数量和文字长度估算页面位置的同步算法。
- 使用本地 PDF.js 读取原始 PDF 当前页文本项及真实坐标，并将右栏段落文本匹配到对应文字行。
- 每个匹配成功的 block 生成独立的精确覆盖区域；匹配失败时不再显示可能误导的估算框。
- P13 实测 7 个 block 均获得独立坐标，第二段、第五段双向点击定位正确。

## 下一步行动

- 扫描件或没有文本层的页面无法获得精确 PDF 坐标，保持不显示错误同步框；未来如需支持，应接入 OCR 返回的真实 bbox。

## 需要跟前端交流的内容

- 同步逻辑现在依赖当前 PDF 的文本层；可编辑 PDF 使用精确坐标，纯图片页面不会回退到猜测位置。

## 各模块同步事项

- 前端 app：本地 AS译林前端资源升级到 v55，完成 P13 七段坐标、第二段、第五段和反向点击测试。
- 管理后台：无需修改。
- 后端服务器：无需修改。
- 数据库：无需修改。
