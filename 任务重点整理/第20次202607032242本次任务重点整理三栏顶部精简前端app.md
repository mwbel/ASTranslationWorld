# 本次任务重点

- 精简三栏顶部按钮的视觉噪音。
- 主操作保留文字按钮：加载文件、识别、翻译。
- 次要操作改为图标按钮，并保留 `title` 与 `aria-label`。
- 同步缩短 OCR 与翻译的忙碌状态文案。

## 下一步行动

- 在浏览器中检查三栏顶部是否不再拥挤。
- 若用户继续反馈拥挤，再进一步合并字体缩放和视图切换控件。

## 需要跟前端交流的内容

- 复制、导出、清空、检查服务、角色管理已转为图标按钮。
- 悬浮提示仍可说明按钮功能。
- OCR / 翻译主按钮继续保持可见文字，降低误操作。

## 需要跟前端 app 同步的内容

- 本次只改按钮布局、文案与样式。
- 不修改 OCR、翻译、缓存、文件路由和接口调用链路。

## 需要跟管理后台同步的内容

- 无。

## 需要跟后端服务器同步的内容

- 无。

## 需要跟数据库同步的内容

- 无。
