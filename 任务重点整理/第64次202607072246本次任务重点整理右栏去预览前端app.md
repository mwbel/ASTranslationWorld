# 第64次 2026-07-07 22:46 本次任务重点整理

## 本次完成的任务重点

- 调整藏文 OCR 校对视图右栏：去掉默认显示的 `原文 block xx 预览` 区块。
- 右栏改为直接按 block 显示 `BDRC block xx 识别` 与 `AI Vision block xx 识别`，优先服务人工校对和选择保存。
- 保留点击 block 同步左栏原文位置的能力，原文预览仍由左栏承担。

## 下一步行动

- 继续观察真实 OCR 数据中 BDRC 与 AI Vision 的差异高亮是否过密。
- 如果右栏仍显拥挤，下一步优先压缩 block 间距和保存按钮布局，不恢复原文预览列表。

## 前端 app

- 已更新 `tibetan-proofreading-app/app.js` 与 `tibetan-proofreading-app/index.html`。
- 下一步可继续优化校对视图的滚动定位和当前 block 选中状态。

## 管理后台

- 本轮无管理后台改动。
- 下一步暂无同步事项。

## 后端服务器

- 本轮无后端服务改动。
- 当前 AI Vision 与模型聚合链路沿用上一轮修复后的 `18092 -> 8890`。

## 数据库

- 本轮无数据库改动。
- 本地浏览器缓存结构未新增字段。
