# 第91次任务重点整理

## 本次完成的任务重点

- 确认用户反馈的“这一排没变”不是编辑框里的角色行，而是右栏顶部 `lang-tabs` 内的角色按钮行。
- 修正 `/Users/Min369/Documents/同步空间/Manju/AIProjects/洞见/sutra-image-package-20260611-2159-server-runtime-amd64/Dockerfile.web-login-patch` 中的本地前端补丁：
  - 保留原有角色管理弹窗隐藏逻辑。
  - 保留新译文编辑器角色行隐藏逻辑。
  - 新增对右栏顶部角色按钮行的同名去重隐藏。
  - 在缺少角色管理元信息时，按同名按钮自动去重，避免“内置/我的”重复同时显示。
- 前端资源版本升级到 `v29`，避免浏览器继续命中旧缓存。
- 已重建并重启本地 `sutra-web`，并用浏览器自动化验证：顶部角色行已从重复显示变为只保留一组可见角色。

## 下一步行动

- 继续验证“点击段落后展开的新译文编辑框”内部角色行在更多页面状态下也保持去重一致。
- 如用户仍希望进一步精简，可再做“默认只显示我的角色，点击后展开全部角色”的二级收纳。

## 需要跟前端交流的内容

- 顶部角色行与编辑框角色行是两个独立 DOM 区域，后续凡是做角色显示策略，都要同时覆盖这两处。
- “隐藏内置角色”不能依赖角色管理弹窗先打开一次，前端应尽量在当前显示区域自给自足地完成去重。

## 前端 app / 管理后台 / 后端服务器 / 数据库同步

- 前端 app：
  - 后续若继续精简角色体验，优先统一顶部角色行、编辑框角色行、角色管理弹窗三处的显示规则。
  - 若要支持“彻底停用内置角色”，需要补明确的状态来源，而不只是 DOM 隐藏。
- 管理后台：
  - 暂无必须改动。
- 后端服务器：
  - 暂无必须改动，本次问题不在接口层。
- 数据库：
  - 暂无必须改动，本次未改角色数据结构。
