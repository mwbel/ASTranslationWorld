# 第52次 2026-07-06 22:31 本次任务重点整理：校对红框预览

## 本次完成的任务重点

- 修复逐 block 校对视图的 AI Vision 行兼容逻辑，旧缓存或不同响应字段下也会优先从 AI Vision 文本还原行内容。
- 在 BDRC 与 AI Vision 可编辑文本上方增加带红框高危字符标注的预览行，保留人工编辑 textarea。
- 调整原文 block 预览 canvas 自适应方式，避免被强制拉伸后裁切。

## 下一步行动

- 用已识别过的藏文 PDF 手动切到“校对”视图，确认每个 block 都按“原文预览、BDRC 识别、AI Vision 识别”显示。
- 如果仍有 AI Vision 空行，检查当前页缓存中 `compare.llm.text` 与 `compare.llm.lines` 是否同时为空，再定位到 AI Vision 服务响应。

## 前端 app

- 继续验证编辑 BDRC/AI Vision 任一版本后，点击“保存修改”能写回当前页 OCR 结果。
- 检查原文 block 预览在窄栏和宽栏下都不裁切。

## 管理后台

- 暂无需要同步。

## 后端服务器

- 暂无后端改动；如 AI Vision 服务本身未返回文本，再检查 `tibetan-ocr-core/ai_vision_ocr_server.py`。

## 数据库

- 未修改数据库或 OCR 缓存结构。
