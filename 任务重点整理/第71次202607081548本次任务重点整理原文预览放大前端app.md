# 第71次 202607081548 本次任务重点整理：原文预览放大

## 本次完成的任务重点

- 将校对视图原文 block 预览改为按同步框 bbox 近似精确裁剪，只保留极小边距。
- 将原文预览的裁剪范围和显示倍率分离，放大同步框中的文字；长行保持放大并允许横向滚动。
- 前端资源版本提升到 `20260708-proofread-blocks-25`，用于强制刷新浏览器缓存。

## 下一步行动

- 在 `http://127.0.0.1:8790/tibetan-proofreading-app/?v=20260708-proofread-blocks-25` 验证原文 block 预览。
- 如果某个 block 仍显示同步框外内容，下一步应检查 OCR 返回的 bbox 是否偏大。

## 前端 app 同步

- 已修改 `tibetan-proofreading-app/app.js`、`tibetan-proofreading-app/styles.css`、`tibetan-proofreading-app/index.html`。
- 原文预览现在优先显示同步框里的文字，并适当放大；长行可横向滚动。

## 管理后台同步

- 无管理后台变更。

## 后端服务器同步

- 无 OCR 后端、AI OCR、翻译服务或模型聚合服务变更。

## 数据库同步

- 无数据库结构或缓存数据迁移。
