# 第126次 2026-08-01 15:51 本次任务重点整理：AS译林独立模板后端

## 本次完成

- 创建了独立的 AS译林 Zeabur 模板。
- 模板已在 Zeabur 侧生成，链接为 `https://zeabur.com/templates/RVMMX5`。
- 已尝试通过模板创建独立项目，Zeabur 拒绝共享集群新建项目，要求使用 `server-XXXXXXXX` 区域码。
- 已将模板部署到现有 `untitled-3` 项目，新增 `redis`、`sutra-server`、`cross-page-service`、`sutra-web`、`as-yilin-model-adapter` 服务。
- 已创建公网域名：`https://as-yilin-mwu.zeabur.app/` 与 `https://as-yilin-adapter-mwu.zeabur.app/`。
- 当前 `as-yilin-mwu.zeabur.app` 返回 `502 Bad Gateway`，原因是 GitHub 构建拿不到被 `.gitignore` 排除的 AS译林运行时二进制和镜像包。
- 已把外部 RDS `DATABASE_URL`、`JWT_SECRET` 写入对应 Zeabur 服务 Variables，未写入仓库。
- 当前检查未发现 `GEMINI_API_KEYS` 已设置到 `as-yilin-model-adapter` 服务；上线翻译前需要在 Zeabur Variables 中补入真实 Gemini key。
- 已修复 adapter 镜像默认配置文件注入，新增标准 `as-yilin-model-adapter/Dockerfile`，并推送到 `origin/main` 与 `mwbel/main`。
- adapter 镜像本地 Docker build 和本地 `/health` 验证通过；Zeabur 服务当前仍显示 `CRASHED`，公网 `/health` 仍返回 `502 Bad Gateway`。
- Zeabur CLI 当前无法读取 adapter 运行日志，`restart` 返回平台内部错误；需要在 Zeabur 控制台查看该服务 Build / Runtime Logs，或重建一个本地上传型 adapter 服务。
- 数据库方案已切换为复用外部 RDS，并使用 `as_yilin` schema。
- `pgcrypto` 已启用，`gen_random_uuid()` 可用。

## 下一步

- 申请或确认可用的 Zeabur `server-...` 区域码。
- 选择 AS译林生产镜像方案：推送本地 AS译林镜像到可用镜像仓库，或改用 Zeabur 支持的本地镜像上传流程。
- 在 Zeabur 控制台查看 `as-yilin-model-adapter` 的 Build / Runtime Logs；若仍无日志，删除该异常 adapter 服务后重新以 `as-yilin-model-adapter/Dockerfile` 创建服务。
- 在 `as-yilin-model-adapter` 服务 Variables 中补入 `GEMINI_API_KEYS`。
- 服务可用后，再把 `astranslationworld.zeabur.app` 从 OCR 服务切换到 AS译林 `sutra-web`。

## 需要同步

- 前端 app：不改。
- 管理后台：不改。
- 后端服务器：确认 `DATABASE_URL` 指向外部 RDS，并保留 `search_path=as_yilin`。
- 数据库：继续沿用外部 RDS，不新建独立 database。
