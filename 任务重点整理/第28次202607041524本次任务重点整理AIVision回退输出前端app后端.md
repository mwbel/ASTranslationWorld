# 第28次 2026-07-04 15:24 本次任务重点整理：AIVision回退输出

## 本次完成的任务重点

- 定位 `tibetan-proofreading-app` 右栏 AI Vision 无输出的真实原因：18092 服务可达，但上游 Gemini 曾返回 429/代理错误，前端只能拿到空结果或失败。
- 修改 `tibetan-ocr-core/ai_vision_ocr_server.py`：ModelAggregatorService 调用默认允许 fallback，不再只锁定单一 Gemini 模型；默认输出 token 上限从 4096 提高到 8192。
- 增加错误脱敏：18092 返回给前端的上游错误会隐藏 Gemini query key 和 Authorization token，避免 UI 或日志暴露密钥。
- 修改 `tibetan-proofreading-app/start_services.sh`：后台服务用 `start_new_session=True` 启动，修正 18092 在脚本健康检查后退出的问题。
- 直接用样本 PDF 第 1 页生成 PNG 测试 18092，AI Vision OCR 返回 200、约 1694 字、23 行，且未检测到 key 泄露。

## 下一步行动

- 前端 app：在页面内重新加载样本 PDF，选择“智能（BDRC + LLM）”，点击识别，确认双栏对照右栏出现 AI Vision 文本；如果上游失败，应显示脱敏后的失败原因。
- 后端服务器：继续观察 ModelAggregatorService 的 Gemini/云雾/Mathpix 候选稳定性；如 Gemini 代理不稳，可通过 `AI_VISION_MODELS` 或 `AI_VISION_MODEL` 指定更稳定模型。
- 管理后台：暂无管理后台改动。
- 数据库：暂无数据库改动。

## 需要同步的内容

- 前端 app 需要把“AI Vision 未返回”理解为上游模型失败或额度/代理问题，不再当成纯 UI 问题。
- 后端服务器需要保留错误脱敏，不允许把模型 API key 带回前端。
- 当前服务地址仍为 `http://127.0.0.1:8790/tibetan-proofreading-app/`，AI OCR 地址为 `http://127.0.0.1:18092/ocr`。
