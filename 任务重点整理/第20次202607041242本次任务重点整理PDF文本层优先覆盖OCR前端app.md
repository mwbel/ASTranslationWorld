# 第20次 2026-07-04 12:42 本次任务重点整理

## 本次完成的任务重点

- 修正可编辑 PDF 文本层与 OCR 缓存的优先级。
- PDF 文本层现在会覆盖 `bdrc`、`ai-vision`、`bdrc-ai`、`cache` 等机器识别结果。
- 保留 `manual` 人工校对文本，不用 PDF 文本层覆盖用户手工修改。

## 下一步行动

- 前端刷新后重新上传同一份可编辑 PDF。
- 检查右栏标题或状态是否显示 `PDF 文本层`。
- 点击翻译当前页，确认翻译输入来自 PDF 文本层而不是旧 OCR 结果。

## 需要跟前端 app 同步的内容

- PDF 文本层是可编辑 PDF 的优先文本来源。
- 扫描件或图片型 PDF 才进入 OCR。
- 已有机器 OCR 缓存不能阻止 PDF 文本层显示。

## 需要跟管理后台同步的内容

- 暂无。

## 需要跟后端服务器同步的内容

- 暂无，本次不改 OCR 或翻译后端。

## 需要跟数据库同步的内容

- 暂无，本次只影响浏览器本地缓存与前端状态。
