# 第32次 202607041712 本次任务重点整理 AIVision报错提示 前端app

## 本次完成的任务重点

- 确认 `18092` 是 AI Vision OCR 服务端口，`18090` 才是 BDRC OCR 服务端口。
- 修正 AI Vision 连接失败时的前端错误提示，改为提示启动 `ai_vision_ocr_server.py` 或运行统一启动脚本。
- 修正智能模式 fallback 中 AI Vision 调用失败时被误归类为 BDRC OCR 失败的问题。

## 下一步行动

- 如果仍出现无法连接 `18092/ocr`，先检查 `curl http://127.0.0.1:18092/health`。
- 若 health 正常但 `/ocr` 失败，再检查上游 ModelAggregatorService 和 Gemini key 轮值状态。

## 前端 app

- 错误提示需要按端口和服务类型区分：`18090` 对应 BDRC，`18092` 对应 AI Vision。

## 管理后台

- 本轮无管理后台改动。

## 后端服务器

- 本轮未改后端；当前 AI Vision 服务 health 已可用时，无需重启 BDRC 服务。

## 数据库

- 本轮无数据库改动。
