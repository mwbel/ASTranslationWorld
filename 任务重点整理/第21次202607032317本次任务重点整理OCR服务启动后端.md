# 本次任务重点

- 定位 BDRC OCR 调用失败的链路位置。
- 确认 `127.0.0.1:18090` 原本没有监听进程。
- 启动本地 OCR 服务：`python3 tibetan-ocr-core/bdrc_ocr_server.py`。
- 验证 `/health` 返回 `ok: true`。

## 下一步行动

- 前端刷新后重新点击“识别”。
- 如果继续失败，下一步检查 `/ocr` POST 请求、上传图片格式、BDRC 模型加载和 OCR 响应解析。

## 需要跟前端交流的内容

- 前端配置的 OCR 地址仍应指向 `http://127.0.0.1:18090/ocr`。
- 之前错误是后端服务未启动，不是按钮或三栏布局问题。

## 需要跟前端 app 同步的内容

- 无需修改前端 app。

## 需要跟管理后台同步的内容

- 无。

## 需要跟后端服务器同步的内容

- 本地 OCR 后端已启动并通过健康检查。
- 进程需要保持运行，关闭终端会导致前端再次无法连接。

## 需要跟数据库同步的内容

- 无。
