# 第69次 202607081211 本次任务重点整理：同步框原文裁剪

## 本次完成的任务重点

- 将校对视图中的 `原文 block 预览` 裁剪范围改为同步框 bbox 周边的小范围内容。
- 移除此前为了看上下文而扩大 crop 的逻辑，避免显示页面标题、上下多行或大段原文。
- 前端资源版本提升为 `20260708-proofread-blocks-22`。

## 下一步行动

- 用户使用 `http://127.0.0.1:8790/tibetan-proofreading-app/?v=20260708-proofread-blocks-22` 刷新验证。
- 如个别 block 的 bbox 来自 OCR 服务且坐标本身偏大，下一步应定位 OCR 返回的 bbox，而不是继续扩大或缩小前端裁剪。

## 前端 app 同步

- 已修改 `tibetan-proofreading-app/app.js`、`tibetan-proofreading-app/styles.css`、`tibetan-proofreading-app/index.html`。
- 原文预览现在只显示同步框对应内容，并保留少量边距避免藏文上下加字被切掉。

## 管理后台同步

- 无管理后台变更。

## 后端服务器同步

- 无 OCR 后端、AI OCR 后端、翻译服务或模型聚合服务变更。

## 数据库同步

- 无数据库结构或缓存数据迁移。
