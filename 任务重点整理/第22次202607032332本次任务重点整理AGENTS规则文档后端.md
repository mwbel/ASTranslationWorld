# 第22次 2026-07-03 23:32 本次任务重点整理：AGENTS规则文档

## 本次完成的任务重点

1. 重写根目录 `AGENTS.md`，参考 `ResearchManjushri/AGENTS.md` 的结构补齐项目目标、工作区边界、执行前规则、项目结构、本地运行入口、测试检查、Git 推送和文档归档规则。
2. 明确区分 AS译林本地副本 / LLM API key 链路与藏文 OCR / 翻译 / 校对工作台链路，避免后续任务跨链路误改。
3. 保留并强化本项目已有约束：Preflight Plan、文本型文档直读、OCR fallback、AS译林本地隔离部署、适配器调试链路、前端冒烟测试和提示词统一归档。

## 下一步行动

1. 后续修改 AS译林或 OCR 工作台前，先按新版 `AGENTS.md` 输出 Preflight Plan。
2. 如果进入 AS译林模型通路调试，优先检查 Docker / Colima / 容器 / BYO key / 适配器 / 上游模型 API 的完整链路。
3. 如果进入 OCR / 翻译工作台调试，优先检查文件路由、文本层抽取、OCR fallback、翻译接口和缓存状态。

## 需要跟前端交流的内容

1. 前端 app 修改必须保护三栏工作台主流程：文件加载、分页、OCR、翻译、人工编辑和导出。
2. 涉及按钮、分页、三栏布局或同步高亮时，需要完成手动前端冒烟测试。

## 需要同步的内容

1. 前端 app：新版规则要求点击失败在当前操作区域暴露具体错误，并保持 Markdown / TXT / 可编辑 PDF 不默认进入 OCR。
2. 管理后台：本轮没有管理后台代码变更。
3. 后端服务器：新版规则明确 AS译林本地副本与适配器调试边界，默认不触碰线上 `https://asyilin.cpolar.top/`。
4. 数据库：本轮没有数据库结构或数据变更。
