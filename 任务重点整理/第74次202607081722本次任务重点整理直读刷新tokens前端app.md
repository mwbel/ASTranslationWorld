# 第74次 2026-07-08 17:22 本次任务重点整理：直读刷新 tokens

## 本次完成的任务重点

- 确认 P1 数据库中 10 个 segment 均存在，且每段都有 source token，下面段落没有在数据层丢失。
- 定位右栏部分段落显示 `0 tokens` 的原因：本地直读走 `/resegment` 后，返回体缺少原应用用于触发 token 重拉的 `token_count`。
- 修复本地前端注入层：直读响应补齐 `token_count` 和 `segment_count`，让原应用在“重新直读”后继续执行 segments/tokens 刷新逻辑。
- 将本地前端资源推进到 `file-router-v21`，避免旧缓存继续使用前一版响应包装。

## 下一步行动

- 前端 app：强制刷新 `http://127.0.0.1:18088/`，对 P1 再点一次“重新直读”，确认右栏 10 个段落都显示正文。
- 管理后台：暂无同步事项。
- 后端服务器：本次未改后端；`/api/segments/{id}/tokens` 已验证可返回正文。
- 数据库：不直接写库；继续通过 `/resegment` 和原应用 token 接口维护一致性。

## 需要同步的内容

- 前端 app：直读接口响应必须带 `token_count > 0`，否则原应用不会立即重拉每段 tokens。
- 管理后台：无。
- 后端服务器：无接口变更。
- 数据库：无 schema 变更。
