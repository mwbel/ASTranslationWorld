# 第91次 202607121116 本次任务重点整理：入口路由分流

## 本次完成的任务重点

- 修复主页两个“新建项目”入口只落到同一个基础地址的问题。
- OCR 入口进入 `?workflow=ocr`，藏译中入口进入 `?workflow=translation`。
- 页面刷新或直接打开带 `workflow` 参数的地址时，能恢复对应工作流视图。

## 下一步行动

- 继续检查“浏览项目”和“继续任务”是否也需要在地址栏保留工作流状态。
- 若后续需要多页面形态，可再拆分为更明确的 `/ocr` 与 `/translation` 路由。

## 前端 app

- 入口按钮、卡片点击和 URL 状态已绑定。
- 前端需要继续验证两个工作流的文件加载、缓存恢复和返回主页行为。

## 管理后台

- 本次不涉及。

## 后端服务器

- 本次不涉及 OCR、翻译或模型聚合服务。

## 数据库

- 本次不涉及。
