# 第58次 2026-07-07 16:49 本次任务重点整理：AIVision显示 前端app

## 本次完成的任务重点

- 修正校对合并视图中 `AI Vision block` 缺失文本时直接留空的问题。
- AI Vision 没有返回真实 OCR 文本时，改为显示明确的未返回或调用失败提示。
- 补充 OpenAI/Gemini 常见响应结构的文本抽取，降低 LLM 已返回但前端解析不到的风险。
- 保留 BDRC 与 AI Vision 真实藏文结果中的高危字符红框与差异标注。

## 下一步行动

- 使用当前样本文档重新点击“识别”，确认 AI Vision 服务实际返回的文本是否进入校对视图。
- 如果仍只显示诊断提示，下一步检查 `tibetan-ocr-core/ai_vision_ocr_server.py` 到 ModelAggregatorService 的调用日志。

## 前端 app

- 校对视图继续保持 `原文 block -> BDRC block -> AI Vision block` 的逐 block 展示顺序。
- AI Vision 诊断提示不可直接作为 OCR 正文保存；用户手动编辑后才允许保存为 AI Vision 版本。

## 管理后台

- 暂无管理后台同步事项。

## 后端服务器

- 后端需确认 `POST /ocr` 返回 `text` 或 `lines` 字段；如果使用 Gemini/OpenAI 兼容结构，前端已补充解析兼容。

## 数据库

- 本轮未改数据库或缓存结构。
