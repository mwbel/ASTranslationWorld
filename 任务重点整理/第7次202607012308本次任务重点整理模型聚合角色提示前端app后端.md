# 第7次任务重点整理：模型聚合角色提示

- 时间：2026-07-01 23:08
- 请求来源：前端 app、后端

## 本次完成的任务重点

- 翻译后端新增 `TRANSLATE_PROVIDER=model_aggregator` 模式，通过 `ModelAggregatorService /api/aggregate/chat` 调用校园网本地模型。
- `/translate` 继续兼容现有字段，并额外接收 `role_id`、`role_name`、`system_prompt`、`user_prompt_template`、`model`、`temperature`、`max_tokens`。
- 三栏工作台翻译栏新增角色选择、角色管理按钮和当前模型显示。
- 前端新增内置角色 `学术直译`、`通顺英译`、`术语拆解`，自定义角色保存在浏览器 `localStorage`。
- 翻译结果仍写入现有 `translationResults`，未改变 OCR、分页和三栏主缓存结构。

## 下一步行动

- 启动 `ModelAggregatorService` 后，用真实模型执行 `/translate` 端到端短文本测试。
- 根据实际翻译质量继续调整内置角色的 system prompt 和 user prompt template。
- 如果后续需要多人共享角色，再考虑从 `localStorage` 迁移到后端数据库或配置文件。

## 需要跟前端交流的内容

- 角色管理弹窗目前是 MVP：内置角色不可删除，保存内置角色会生成自定义副本。
- 前端应确认角色选择、重置、自定义角色保存后的交互是否符合使用习惯。
- 前端 app 下一步：完成浏览器手动冒烟，确认刷新后角色和翻译缓存恢复。

## 需要同步的内容

- 前端 app：新增翻译角色 UI 和 localStorage 管理，不改变 OCR 对照交互。
- 管理后台：本次不涉及。
- 后端服务器：`tibetan-translation-services` 需要设置 `TRANSLATE_PROVIDER=model_aggregator`、`MODEL_AGGREGATOR_BASE_URL`、`MODEL_AGGREGATOR_MODEL`。
- 数据库：本次不涉及，不保存校园网模型 API key。
