# 第43次 2026-07-05 19:43 本次任务重点整理：清理旧OCR残留

## 本次完成的任务重点

- 定位到 AS译林本地副本右栏显示旧 OCR 的原因：当前 PDF 第 1 页仍挂有上一轮藏文 OCR 的 `segments/source_tokens`，并非当前中文 PDF 文本层提取结果。
- 已备份旧数据到 `/tmp/as_yilin_stale_ocr_page1_20260705193125.jsonl`。
- 已清除当前页残留的 1 个旧 `segment` 和 4 个 `source_tokens`。
- 已将当前页 OCR 状态重置为 `pending`。
- 验证当前 PDF 章节 564 页均为 `pending`，段落数为 0。

## 下一步行动

- 刷新 `http://127.0.0.1:18088/` 后重新进入该 PDF 页面，确认右栏不再显示旧藏文 OCR。
- 下一步应补齐“切换/重新加载文档时自动清理旧段落或明确提示复用”的前端与后端保护逻辑，避免后续再出现跨文档残留。

## 前端 app

- 当前无需改代码；用户刷新页面后应看到右栏等待原典，不再显示旧 OCR 段落。
- 后续需要在上传新文档或重新导入文档时，明确显示“旧段落已清空 / 复用旧段落”状态。

## 管理后台

- 暂无管理后台动作。

## 后端服务器

- 本次只处理本地数据库数据，不修改 AS译林后端服务代码。
- 后续可在导入链路加保护：同一 page 重建来源文件时，应同步清理旧 `segments` 或生成新的 page 记录。

## 数据库

- 已对本地 `sutra_local` 数据库执行清理。
- 未删除源 PDF、项目、章节或页面记录。
- 未影响线上 `https://asyilin.cpolar.top/`。
