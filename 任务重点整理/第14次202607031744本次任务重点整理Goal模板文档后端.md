# 第14次 2026-07-03 17:44 本次任务重点整理：Goal模板文档

## 本次完成的任务重点

1. 新增项目根目录 `GOAL模板.md`。
2. 将 `/goal` 使用原则整理为可复用说明。
3. 按 `bug 修复 / 重构 / 上线前收敛 / 功能开发` 四类落成可直接复制的模板。
4. 保留 4 条日常短版 `/goal`，用于快速启动单一主目标。

## 下一步行动

1. 后续启动持续执行任务时，优先从 `GOAL模板.md` 复制模板并替换占位内容。
2. 如果任务范围仍然模糊，先用 `/plan` 收敛完成标准，再开 `/goal`。
3. 如果后续 AS译林或 OCR / 翻译工作台链路规则变化，再同步更新 `GOAL模板.md`。

## 需要跟前端交流的内容

本次只新增开发协作文档，不涉及前端页面、按钮、布局或交互变更。

## 需要跟前端 app、管理后台、后端服务器、数据库同步的内容

### 前端 app

暂无同步事项。

### 管理后台

暂无同步事项。

### 后端服务器

后端相关任务后续可直接引用 `GOAL模板.md` 中的 AS译林本地链路和 adapter / 聚合器定位模板。

### 数据库

暂无同步事项。
