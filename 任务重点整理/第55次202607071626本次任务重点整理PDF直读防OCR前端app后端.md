# 第55次 2026-07-07 16:26 本次任务重点整理：PDF直读防OCR

## 本次完成的任务重点

- 定位 AS译林本地副本 P2 错误：当前页是可编辑 PDF，但 `重新 OCR / 全部识别` 仍调用 OCR 接口，写入了藏文乱码 `segments/source_tokens`。
- 已备份 P2 错误 OCR 数据到 `/tmp/as_yilin_p2_wrong_ocr_20260707161917.jsonl`。
- 已用服务端 `pdftotext` 从原 PDF 第 2 页抽取中文文本，并通过 `/api/pages/{id}/resegment` 写回 P2。
- 验证 P2 当前为 23 个中文分段，`source_tokens.lang=zh`。
- 已修改本地 AS译林 web 补丁：对可编辑 PDF 页拦截 `/api/pages/{id}/ocr` 和 `/api/chapters/{id}/ocr-all`，改为抽取 PDF 文本层并调用 `resegment`，避免再次误 OCR。

## 下一步行动

- 用户刷新 `http://127.0.0.1:18088/` 后进入 P2，确认右栏显示中文直读分段。
- 后续如果其它 PDF 页仍显示旧 OCR 残留，应按同一链路清理并由新前端路由阻止再次产生。

## 前端 app

- 已更新本地 web 注入补丁，当前构建资产包含 PDF 文本层 OCR 拦截逻辑。
- 前端按钮仍显示原应用的 OCR 文案，但实际请求会优先走文本层直读。

## 管理后台

- 暂无管理后台动作。

## 后端服务器

- 本次未修改 AS译林后端二进制服务。
- 使用既有 `/api/pages/{id}/resegment` 接口写回当前页文本分段。

## 数据库

- P2 错误 OCR 数据已备份。
- P2 当前数据已从藏文 OCR token 改为中文 `zh` token。
- 未影响线上 `https://asyilin.cpolar.top/`。
