# 第86次 2026-07-11 13:55 本次任务重点整理：AS译林模型通路修复

## 本次完成的任务重点

1. 先直连 `ModelAggregatorService /api/aggregate/chat` 验证模型真实表现：
   - 有内容返回：`local:qwen2.5:14b`、`local:gemma2:27b`、`gemini:gemini-2.5-flash`。
   - 上游空响应或超时：`local:qwen3.6-27b:latest`、`local:batiai/qwen3.6-27b:q4`、`gemini:gemini-3-flash-preview`。
   - `yunwu:gpt-5.5` 的上游模型列表实际返回本地模型，没有 `gpt-5.5`，因此没有接入 AS译林模型列表。
2. 更新 `as-yilin-model-adapter` 的模型配置：每个已验证模型使用独立的 provider-qualified `upstream_model`，当前 AS译林 `/v1/models` 暴露 3 个模型：
   - `agg-local-qwen25` → `local:qwen2.5:14b`
   - `agg-local-gemma2` → `local:gemma2:27b`
   - `agg-gemini-25-flash` → `gemini:gemini-2.5-flash`
3. 将上游空响应模型保留在配置但设为 `enabled: false`，避免 AS译林展示不可用模型。
4. 修复适配器未知模型静默回退问题：未知模型或禁用模型现在返回明确的 502 错误，不会悄悄调用 Qwen2.5。
5. 适配器收到聚合器 `ok=true` 但空文本时，现在也返回明确的 ProviderError。
6. 重建并重启本地 `as-yilin-model-adapter` Docker 容器。

## 下一步行动

1. 在本地 AS译林 `http://127.0.0.1:18088/` 中重新拉取模型，确认角色配置改用新的 `agg-*` 模型 ID。
2. 分别用一个短段落验证 Qwen2.5、Gemma2、Gemini 2.5 Flash 的真实翻译结果。
3. 如果上游修复 Qwen3.6、Qwen3.6 Q4 或 Gemini 3 的空响应，再把对应配置从 `enabled: false` 恢复为 `true`，并重新做内容非空验证。

## 需要跟前端 app、管理后台、后端服务器、数据库同步的内容

### 前端 app

- 不修改 AS译林前端代码。
- 重新拉模型后，角色应选择 `agg-local-qwen25`、`agg-local-gemma2` 或 `agg-gemini-25-flash`。
- 旧的 `gemini-3-flash-preview`、`gpt-5.4` 等名称不再伪装成 Qwen2.5；调用时会收到明确错误。

### 管理后台

- 暂无同步事项。

### 后端服务器

- ModelAggregatorService 仍运行在 `http://127.0.0.1:8890`。
- AS译林适配器仍运行在 `http://127.0.0.1:18081`，继续通过 `/api/aggregate/chat` 转发。

### 数据库

- 未修改数据库结构或业务数据。
- 未修改 AS译林线上部署。
