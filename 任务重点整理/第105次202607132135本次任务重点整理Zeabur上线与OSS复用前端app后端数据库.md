# 第105次任务重点整理：Zeabur 上线与 OSS 复用

## 本次完成的任务重点

- 复用参考项目 Zeabur CLI 登录态，在同一 Zeabur 项目的 production 环境新增藏文校对服务。
- 上线地址为 `https://tibetan-proofreading.zeabur.app/tibetan-proofreading-app/`。
- 参考 RAM 凭证无新建 Bucket 权限，因此复用原私有 Bucket，并用 `tibetan-proofreading/` 前缀隔离本项目对象。
- OSS 建书、状态读写、导出回写、AI Vision OCR 和藏译中真实调用均已验证。

## 下一步行动

- 部署独立 Linux/GPU BDRC OCR 服务，并把 `/ocr` 地址填入 `BDRC_OCR_UPSTREAM_URL`，完成 BDRC + LLM 真正双结果对照。
- 增加书库列表和按 `book_id` 恢复源书，支持跨设备继续校对。
- 正式交给多人前补充用户身份、书籍 owner 和管理员权限。

## 需要跟前端交流的内容

- 云端自动使用同域 API；智能 OCR 在 BDRC 未配置时会回退到 AI Vision。
- 不应把当前回退模式描述为已经完成 BDRC 与 LLM 双结果对照。

## 需要同步的内容

- 前端 app：线上页面可访问，后续增加书库恢复入口。
- 管理后台：后续增加书籍分配和状态筛选。
- 后端服务器：OSS、AI OCR、翻译已上线；BDRC 上游待部署。
- 数据库：当前状态保存在 OSS JSON；正式多人使用前建议迁移 Postgres。
