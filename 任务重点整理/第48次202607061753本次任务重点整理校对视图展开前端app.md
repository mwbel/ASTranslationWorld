# 第48次 2026-07-06 17:53 本次任务重点整理

## 本次完成的任务重点

- 修正 `校对` 视图中 block 卡片容易被压成细条的问题。
- 校对卡片改为显式展开结构，保留原文 block 预览、BDRC 编辑区、AI Vision 编辑区。
- 在校对模式下隐藏左栏全量 block 虚线 overlay，避免原文页被大量同步框遮挡。
- 保留点击 block 后的单个红色定位框，用于原文与校对结果同步。

## 下一步行动

- 用当前藏文 PDF 检查真实 OCR 缓存页面，确认每个 block 的三段内容都能展开显示。
- 如原文裁切预览过低，再只微调预览高度和裁切 padding。

## 前端 app

- 需要继续观察 `校对`、`对照`、`逐行`、`文本` 四种视图切换后左栏同步框是否符合预期。

## 管理后台

- 暂无同步事项。

## 后端服务器

- 未修改后端服务；后端无需同步。

## 数据库

- 暂无数据库改动。
