# 第104次任务重点整理：末尾原文显示恢复

## 本次完成的任务重点

- 确认问题发生在前端显示层：PDF 文本层、段落记录和末尾三段 `source_tokens` 均已正常写入数据库。
- 在本地 AS译林 Web 补丁中增加缺失原文恢复逻辑：段落卡片没有 `.segment-source-preview` 时，按 `segment_id` 重新读取已有 tokens 并补回显示。
- 修复只操作浏览器 DOM，不重新 OCR、不重分段、不修改数据库或已有译文。
- 前端资源升级为 `index-ex7lBOJf-file-router-v35.js`，Service Worker 缓存键升级为 `as-yilin-v35-source-token-display-recovery`。

## 下一步行动

- 用户刷新 `http://127.0.0.1:18088/`，进入原页面确认“释迦牟尼佛……”“无奈的是……”和“其实不用……”三段均在右栏显示。
- 若个别卡片仍为空，继续核对该卡片的 `segment_id` 与 `/api/segments/{id}/tokens` 返回，不修改 PDF 提取或数据库结构。

## 需要跟前端交流的内容

- 原文丢失属于显示层缺口；恢复逻辑只在卡片缺少原文预览且接口确有 tokens 时触发。
- 正常卡片不受影响，翻译、确认和已有译文入口保持原逻辑。

## 各模块同步事项

- 前端 app：已加入缺失 source tokens 的显示恢复逻辑并更新本地静态资源版本。
- 管理后台：无需修改。
- 后端服务器：无需修改，现有 tokens 接口继续使用。
- 数据库：无需修改，现有段落和 tokens 保持不变。
