# 第34次 202607042203 本次任务重点整理：原文块同步

## 本次完成的任务重点

- 将 AS译林本地前端第一栏 `.text-source-viewer__segment` 改为独立 block 卡片。
- 增加本地前端同步器：按顺序读取右栏 `.segments-list .segment-card` 高度，并写入对应原文 block 的 `--sutra-sync-height`。
- 通过 JS 注入 `sutra-local-block-sync-styles`，避免原 CSS 文件缓存导致样式不生效。
- 本地 bundle 升级为 `index-ex7lBOJf-file-router-v8.js`，service worker 升级为 `as-yilin-v8-local-text-layer-router`。

## 下一步行动

- 如后续需要更强同步，可增加左右栏滚动联动；本次只做 block 高度和视觉框同步，不改变滚动逻辑。
- 继续测试点击原文 block 是否能稳定跳到对应译文段。

## 需要跟前端 app 同步的内容

- 第一栏原文段落已从扁平行列表变成卡片式同步框。
- 冒烟验证：`洞见 / 未分章 / P1` 前 6 个原文 block 均与右栏对应译文 block 高度一致，第一栏仍保留直读文本结果。
- 干净浏览器验证无新增 console warn/error。

## 需要跟管理后台同步的内容

- 无管理后台改动。

## 需要跟后端服务器同步的内容

- 无 AS 后端代码改动。
- 本次只修改本地 web patch，不改变段落、翻译、OCR 或 LLM 接口。

## 需要跟数据库同步的内容

- 未修改数据库。
- 原文 block 同步完全基于前端 DOM 渲染层完成。
