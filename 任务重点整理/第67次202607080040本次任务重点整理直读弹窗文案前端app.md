# 第67次 2026-07-08 00:40 本次任务重点整理：直读弹窗文案（前端app）

## 本次完成的任务重点

- 修正 AS译林本地副本 web 镜像补丁中的 `重新直读 / 全部直读` 相关前端文案。
- 将批量确认弹窗从 `pending 页跑 OCR / 每页一次 LLM Vision 调用` 改为 `执行文本层直读 / 优先读取 PDF 文本层；只有无有效文本层的页面才会回退识别`。
- 将前端资源版本从 `file-router-v15` 提升到 `file-router-v16`，避免浏览器继续加载旧 bundle。
- 已重建并重启本地 `sutra-web` 容器。

## 下一步行动

- 前端 app：后续若仍有 `OCR` 字样，应区分是“扫描件 fallback 识别”还是“文本型 PDF 直读”，避免按钮和弹窗混用。
- 管理后台：暂无动作。
- 后端服务器：本次没有修改接口行为，`/api/chapters/:id/ocr-all` 仍由本地前端补丁拦截后优先走 PDF 文本层直读。
- 数据库：暂无结构变更，不需要迁移。

## 需要同步的内容

- 前端 app：AS译林本地页面已加载 `/assets/index-ex7lBOJf-file-router-v16.js`。
- 后端服务器：本次只改 web 镜像补丁，不改 Rust 服务和数据库。
- 管理后台：本次没有涉及。
- 数据库：本次没有涉及。
