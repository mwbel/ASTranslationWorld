# 第77次 2026-07-10 15:23 本次任务重点整理：原文预览缩放 前端app

## 本次完成的任务重点

- 在藏文 OCR 校对视图的原文 block 预览标题栏增加缩小 / 放大按钮。
- 新增原文预览独立缩放状态，不影响 BDRC / AI Vision 识别文本已有字体缩放。
- 缩放状态写入浏览器本地存储，刷新后保持。
- 更新前端资源版本号，避免浏览器缓存旧 JS / CSS。

## 下一步行动

- 在端口页面加载样本文档后，进入“校对”视图，检查原文 block 预览缩放按钮是否符合阅读节奏。
- 如果用户希望缩放只作用于当前 block，而不是所有 block，再改为 block 级状态。

## 前端 app 同步

- 前端需要关注原文预览 canvas 在窄窗口下是否仍保持自适应。
- BDRC / AI Vision 文本缩放按钮继续沿用原有 OCR 字体缩放逻辑。

## 管理后台同步

- 无管理后台变更。

## 后端服务器同步

- 无 OCR 后端、AI Vision 后端、翻译后端变更。

## 数据库同步

- 无数据库变更；本次只使用浏览器 localStorage 保存 UI 缩放偏好。
