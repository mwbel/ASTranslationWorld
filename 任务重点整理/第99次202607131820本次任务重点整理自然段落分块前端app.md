# 第99次任务重点整理：自然段落分块

## 本次完成的任务重点

- 将 PDF.js 直读结果从仅保留文本行升级为同时保留行横纵坐标，以段首缩进和明显较大行距恢复自然段边界。
- 取消遇到句号、问号等句末标点立即生成 block 的规则，改为只在自然段边界、标题或独立短行处切分。
- 保留章节标题独立成块的既有规则，避免标题与正文黏连。
- 本地前端补丁升级为 v34，并重建本地 Compose 服务。
- 使用 P64 重新直读验证：“射手假说”和“农场主假说”的多句话分别保持为完整段落 block；P64 从 8 个 block 调整为 7 个。

## 下一步行动

- 后续新直读或手动“重新直读”的 PDF 页面会采用自然段落分块；既有页面不会自动批量重写。
- 如需让历史页面统一采用新规则，可逐页重新直读，或另行设计受控的批量重分段操作。
- 继续观察复杂双栏、列表或无明显缩进的 PDF；若出现误合并，只调整 PDF 段落边界识别，不扩展修改到 OCR 或翻译服务。

## 需要跟前端交流的内容

- 右栏 block 的语义现在是“自然段”，不再是“单句”。
- 标题仍保持独立，正文段落中的多个句子会一起交给翻译模型。

## 各模块同步事项

- 前端 app：v34 自然段落分块已在 P64 完成真实浏览器验证。
- 管理后台：无需修改或同步。
- 后端服务器：继续接收 `split_mode: line`，但前端传入的每一行已是完整自然段；无需改后端接口。
- 数据库：仅 P64 因冒烟测试重新生成本地段落结构；未调整数据库结构。
