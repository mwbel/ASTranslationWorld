# 第33次 202607041951 本次任务重点整理：直读页优先

## 本次完成的任务重点

- 确认 `洞见 / 未分章 / P1` 右栏错字来源不是 OCR 服务，而是前端仍选中旧 `pdf_page / tesseract` 页面。
- 本地 AS译林前端 patch 增加章节页列表修正：同一章节存在有效 `text / text_import` 页面时，隐藏旧 PDF/OCR 页，并把旧页段落请求重定向到直读文本页。
- 生成并引用 `index-ex7lBOJf-file-router-v6.js`，同步更新 service worker 版本，避免浏览器继续加载旧 `file-router-v3` 缓存。
- 已重建并启动本地 `sutra-local-web`，本地入口仍为 `http://127.0.0.1:18088/`。

## 下一步行动

- 继续打通右栏翻译/AI 生成链路时，优先检查模型聚合器与 AS 后端适配器，不再把文本错误误判为 OCR 问题。
- 如果要彻底清理旧数据，再单独规划数据库归档/删除策略；本次未删除旧 `pdf_page` 或 OCR 缓存。

## 需要跟前端 app 同步的内容

- 前端 app 已加载 `file-router-v6.js`。
- 冒烟验证：`洞见 / 未分章 / P1` 显示“原文文本 · P1”，左栏与右栏均为 `编序 / 萦绕 / 辉煌 / 璀璨` 等直读文本，不再显示 `缠序 / 蒙绕 / 瘤烛 / 璀焉`。
- 干净刷新后无新增 console warn/error。

## 需要跟管理后台同步的内容

- 无管理后台改动。
- 当前修复只影响本地前端运行态路由，不改变用户、项目或权限配置。

## 需要跟后端服务器同步的内容

- 无 AS 后端代码改动。
- 后端数据库中旧 `pdf_page / tesseract` 页面仍存在；前端本地 patch 会在渲染层优先使用直读文本页。

## 需要跟数据库同步的内容

- 未修改数据库。
- 已确认 `未分章` 章节里仍有旧 OCR 页与 text 页并存；这是本次前端路由修复的输入条件。
