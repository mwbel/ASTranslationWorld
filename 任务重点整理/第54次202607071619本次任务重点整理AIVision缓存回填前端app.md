# 第54次 2026-07-07 16:19 本次任务重点整理：AI Vision 缓存回填

## 本次完成的任务重点

- 修复旧缓存中 `compare.llm` 为空时不再读取 `result.raw.ai` 的问题。
- 增加 raw 结果回填逻辑：如果 `compare` 缺少 AI Vision 内容，会从 `raw.ai` 重新提取文本和行数据。
- 校对视图中 AI Vision 为空时显示明确占位，不再表现为空白区域。
- 更新前端资源版本号，避免浏览器继续使用旧 `app.js`。

## 下一步行动

- 在浏览器中强制刷新页面后，重新打开已识别页面的“校对”视图。
- 如果 AI Vision 仍为空，需要检查当前页缓存内是否确实存在 `raw.ai` 或重新点击“识别”生成 AI Vision 结果。

## 前端 app

- 校对卡片保留“原文 block 预览 -> BDRC block 预览 -> AI Vision block 预览”的纵向结构。
- BDRC 与 AI Vision 结果区仍可编辑，失焦后重新渲染高危红框。

## 管理后台

- 暂无需要同步。

## 后端服务器

- 暂无后端改动；若 `raw.ai` 本身为空，再检查 `tibetan-ocr-core/ai_vision_ocr_server.py` 或上游模型服务。

## 数据库

- 未修改数据库或 OCR 缓存结构；仅在前端读取旧缓存时做兼容回填。
