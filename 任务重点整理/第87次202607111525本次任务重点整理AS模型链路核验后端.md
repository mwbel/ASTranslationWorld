# 第87次 202607111525 本次任务重点整理：AS模型链路核验

## 本次完成的任务重点

1. 重新核验本地 `Model Aggregator Service` 直调状态：
   - `qwen2.5:14b`
   - `qwen3.6-27b:latest`
   - `batiai/qwen3.6-27b:q4`
   - `gemma2:27b`
   - `llama3:latest`
   - `gemma4:e4b`
   均返回成功，不再停留在旧的超时结论。
2. 核验运行中的 `as-yilin-model-adapter` 已加载多模型配置，而不是单模型别名回退：
   - `/models` 当前暴露 5 个模型
   - `agg-local-qwen25`
   - `agg-local-qwen36`
   - `agg-local-gemma2`
   - `agg-local-qwen36-q4`
   - `agg-gemini-25-flash`
3. 核验适配器逐模型调用成功：
   - `agg-local-qwen25`
   - `agg-local-qwen36`
   - `agg-local-gemma2`
   - `agg-local-qwen36-q4`
   均可经 `18081` 返回译文。
4. 核验本地 AS译林 副本仍在线：
   - `http://127.0.0.1:18088/` 返回 `200 OK`
   - `sutra-local-web`、`sutra-local-server`、`sutra-local-postgres`、`sutra-local-redis` 均处于运行状态
5. 核验 AS 本地数据库中的 `gv-reseller` 入口仍指向：
   - `http://host.docker.internal:18081`

## 下一步行动

1. 在 AS译林 前端角色管理中执行一次“重新拉模型”，确认界面刷新为 5 个真实模型。
2. 逐个将角色挂到明确模型，不再复用“模型名很多、上游其实同一个”的旧映射方式。
3. 对“学术直译”“终稿润色”“疑难段落”做一次端到端人工冒烟，确认前端实际落到目标模型。

## 需要跟前端交流的内容

- 角色管理弹窗里建议优先显示可理解的模型名，例如 `Qwen3.6 27B`、`Gemma2 27B`，减少 `agg-*` 前缀对非技术用户的干扰。
- 如果“重新拉模型”后仍显示旧模型，需要在当前区域给出明确提示，而不是静默失败。

## 前端app、管理后台、后端服务器、数据库各自下一步

- 前端 app：
  - 验证角色管理中的模型刷新结果是否与适配器 `/models` 一致。
  - 验证切换不同角色后，前端发出的模型 ID 是否发生变化。
- 管理后台：
  - 当前无需改动；继续沿用现有角色管理入口。
- 后端服务器：
  - 保持 `as-yilin-model-adapter` 当前多模型配置，不再把强模型名称伪装成 `qwen2.5:14b`。
  - 后续如新增模型，只按“聚合器直调成功 -> 适配器独立映射 -> AS 角色接入”的顺序处理。
- 数据库：
  - 保持 `user_gv_credentials.gateway_endpoint = http://host.docker.internal:18081`。
  - 若用户后续更换适配器端口或域名，再做单点配置变更，不扩散到业务代码。
