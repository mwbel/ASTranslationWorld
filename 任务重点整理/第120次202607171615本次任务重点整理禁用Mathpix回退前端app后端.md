# 第120次任务重点整理

## 本次完成的任务重点

- 查明 AI Vision 请求 Gemini 失败后，模型聚合器因允许回退而选择了 Mathpix。
- 将藏文 AI Vision 默认模型固定为 `gemini:gemini-2.5-flash`，默认关闭跨模型回退。
- 后端与前端同时拒绝 Mathpix 响应，防止公式 OCR 结果写入藏文 OCR 缓存。

## 下一步行动

- 前端 app：重新识别受影响页面，以 Gemini 结果替换页面中已有的 Mathpix 历史结果。
- 管理后台：无需调整。
- 后端服务器：保持 18092 AI Vision 服务的 `AI_VISION_ALLOW_FALLBACK=0`。
- 数据库：本次不修改数据库或既有 OCR 数据；历史 Mathpix 结果仅在用户重新识别后由正常流程更新。

## 同步事项

- 模型聚合器继续提供 Gemini 调用和 API key 池能力，但藏文 AI Vision 不再接受 Mathpix 回退。
- Gemini 不可用时应返回明确失败信息，不应返回其他领域 OCR 的伪成功结果。
