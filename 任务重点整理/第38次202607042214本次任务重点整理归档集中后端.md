# 第38次 202607042214 本次任务重点整理：归档集中

## 本次完成的任务重点

- 新建并使用 `任务重点整理/` 文件夹，集中保存第二类任务重点整理文档。
- 将项目根目录下散落的 `第*次*本次任务重点整理*.md` 文件移动到 `任务重点整理/`。
- 更新 `AGENTS.md`：后续第二类文档必须写入 `任务重点整理/`，不再散落项目根目录。

## 下一步行动

- 后续每次任务归档时，继续把提示词写入根目录 `提示词历史.md`。
- 后续每次任务重点整理文档，统一写入 `任务重点整理/第X次...本次任务重点整理...md`。

## 需要跟前端 app 同步的内容

- 无前端 app 改动。
- 下一步：前端相关任务归档也放入 `任务重点整理/`。

## 需要跟管理后台同步的内容

- 无管理后台改动。
- 下一步：管理后台相关任务归档也放入 `任务重点整理/`。

## 需要跟后端服务器同步的内容

- 无后端服务器改动。
- 下一步：后端相关任务归档也放入 `任务重点整理/`。

## 需要跟数据库同步的内容

- 无数据库改动。
- 下一步：数据库相关任务归档也放入 `任务重点整理/`。
