# 第121次任务重点整理

## 本次完成的任务重点

- 将 OCR 工具栏现有导出入口明确改为 Markdown 下载，避免增加重复按钮。
- 按页汇总当前已保存的最终 OCR 校对文本，生成 UTF-8 Markdown 文档。
- Markdown 文件包含文档标题、页码二级标题和每页校对正文，并使用“藏文OCR校对结果”文件名。

## 下一步行动

- 前端 app：继续校对时先保存各 block 的采用版本，再下载 Markdown，确保导出的是最终选择。
- 管理后台：无需调整。
- 后端服务器：无需调整；导出由浏览器本地完成。
- 数据库：不新增表，不改变 OCR 缓存结构。

## 同步事项

- Markdown 下载只读取当前项目已经保存的 OCR 最终文本，不修改 BDRC 或 AI Vision 原始结果。
- 未识别或没有文本的页面不会生成空白章节。
