# 提示词历史

按时间顺序整理。未记录具体时间的条目标记为 `时间未记录`。

## 2026-07-01

- `20:33` 从文件加载 智能 选择 md 格式的文件
- `20:45` 上传文件之后看不到文件
- `21:00` 上传文件没有任何相应
- `21:37` 已经完成 Hugging Face NLLB API 配置，接入右栏藏译中接口。
- `时间未记录` 点击“从文件加载”没有任何响应
- `22:02` 接下来看 http://127.0.0.1:18088/ 中的翻译功能。作为MVP，我们先以API的方式调用开源的中翻英模型
- `时间未记录` 好的，按建议执行
- `时间未记录` 1. 左栏的同步框高度太低了，把文字给挡住了 2. 右栏 识别结果没有必要出现两次识别结果，高危识别 可以 直接在上面一行的显示结果上 用红框 和红字 做标记。
- `时间未记录` 每一行都是高危，那是不是就失去了标注高危的意义。应该重点标注前面讲的高危字符
- `23:08` PLEASE IMPLEMENT THIS PLAN: 接入 ModelAggregatorService 并新增角色提示词管理。在 tibetan-proofreading-app 三栏工作台新增翻译角色能力，并让 tibetan-translation-services 通过 ModelAggregatorService /api/aggregate/chat 统一调用校园网本地模型；新增 model_aggregator provider、角色管理 UI、localStorage 角色配置、翻译请求角色参数、健康检查提示和项目记录文档。

## 2026-07-02

- `时间未记录` 窗口没有缩小太多，但是两栏变成了上下布局
- `21:54` 希望把通过 LLM API key 访问的 AS译林应用尽快跑通。请参考另一个项目的 AGENTS.md 更新本项目的 AGENTS.md。
- `23:16` 请逐步打通各个 API 接口。

## 2026-07-03

- `11:08` 用户反馈 adapter 容器内 `HF_TOKEN=missing`，要求继续检查 Hugging Face token 注入与 AS译林本地 LLM API 调用链路。
- `11:51` 用户决定不再直接使用 Hugging Face Router，改为通过自己实现的 `ModelAggregatorService` 作为 AS译林本地 LLM 聚合通路。
- `15:11` 提示词统一写入一个文档。
- `17:44` 把适合本项目的 `/goal` 模板直接落成项目内一份可复用文档。
- `17:49` 用户指出 ModelAggregatorService 本地模型全都报错，并提供服务器 OpenAI-compatible URL 与 API key，要求重新检查本地模型调用链路。
- `18:10` 将 AS译林接上模型聚合器连接。
- `20:51` 让本地部署到的AS译林可以跑通翻译功能；继续。
- `时间未记录` 中栏已经对复杂藏文字母做了标记，是否可以在原文的相应位置同样标出？
- `21:37` 我现在上传的文件是纯pdf，这个文件不需要OCR。之前说过要构建一个文件路由器。
- `21:59` 提示词合并到一个文档，用时间做分割
## 2026-07-03 第9次对话提示词

上述要求的“估算出原文中的对应位置并画红色小框” ，没有画红色小框
## 2026-07-03 第10次对话提示词

左栏的文字完全看不清，而且划分有重叠。如果无法解决，还是去掉做栏的红框
## 2026-07-03 第11次对话提示词

左栏的同步框会漂移
## 2026-07-03 第12次对话提示词

这里的OCR是调用的本地模型还是智能方式。此外，我之前说过，当原文是 文字可编辑的PDF文件时，不需要调用OCR来识别。

## 2026-07-03 第13次对话提示词

- `22:42` 三栏的顶部按钮太吵了。请精简。

## 2026-07-03 第14次对话提示词

- `23:17` BDRC OCR 调用失败：无法连接 `http://127.0.0.1:18090/ocr`，需要先启动本地 OCR 服务。

## 2026-07-03 第15次对话提示词

- `23:59` 右栏文字仍来自 OCR 导致错别字；要求识别左栏源文档属性，如果是 PDF 或 Word 则直接提取文字，例如当前源文档是 PDF。

## 2026-07-03 第22次对话提示词

- `23:32` 参考另一个项目的 AGENTS.md 文档，为本项目撰写一份；中断后继续完成当前项目根目录 AGENTS.md。
## 2026-07-03 第13次对话提示词

参考这个，对藏文识别也从本地BDRC增加到搭配LLM的识别
## 2026-07-03 第14次对话提示词

继续实现藏文 OCR 从“仅本地 BDRC”扩展为可选“智能（BDRC+LLM）/仅本地 BDRC/仅 AI Vision”的识别模式，并测试可行性
## 2026-07-04 第15次对话提示词

右边标记出来很好，识别准确率一目了然。但为了便于编辑，每行文字是不是额外复制一份？

## 2026-07-04 第16次对话提示词

增加一个 BDRC 和 LLM的双栏识别对照

## 2026-07-04 第17次对话提示词

我没有看到BDRC 和 AI Vision 的对照

## 2026-07-04 第18次对话提示词

- `11:36` 继续当前目标：1. 实现藏文 OCR 从“仅本地 BDRC”扩展为可选“智能（BDRC+LLM）/仅本地 BDRC/仅 AI Vision”的识别模式；2. 通过提示词配置一个翻译角色，例如宗萨钦哲仁波切英翻中译文风格；3. 测试上述两个目标的完成情况。

## 2026-07-04 第19次对话提示词

做成双栏对照

## 2026-07-04 第20次对话提示词

目前先专注OCR，先隐藏翻译能力

## 2026-07-04 第20次对话提示词

- `12:42` 右栏的结果依然是 OCR 识别结果；截图显示可编辑 PDF 左栏文本清晰，但右栏仍显示 OCR 产生的错别字结果，要求继续修正 PDF 文本层直读优先级。

## 2026-07-04 第21次对话提示词

- `12:56` AS译林前端截图显示“AI 翻译失败”：`LLM gateway error 502 Bad Gateway`，上游 `1cnumodel.cpolar.top` 超时且 `/v1/chat/completions` 返回路径错误提示；要求继续打通本地 AS译林的 LLM 翻译链路。

## 2026-07-04 第23次对话提示词

- `13:07` 目前的结果依然是 OCR 识别的，有错别字。截图显示 AS译林本地页面中可编辑中文 PDF 左侧原文为“编序”，右侧结果仍出现 OCR 错字。

## 2026-07-04 第24次对话提示词

- `14:25` 用户贴出右栏识别结果仍含 OCR 错字：`缠序`、`蒙绕`、`瘤烛`、`璀焉`、`未法`、`皖拜`、`性悔`、`效罗门`、`一瘟灯` 等，要求继续修正 AS译林本地副本中文可编辑 PDF 的直读链路。

## 2026-07-04 第25次对话提示词

- `14:39` 用户截图指出 AS译林本地前端最左侧项目/页面导航栏宽度固定，希望左边栏也设置为可以调节宽度。

## 2026-07-04 第26次对话提示词

- `14:30` 右栏的 AI Vision 没有任何输出。截图显示 `tibetan-proofreading-app` 的 BDRC / AI Vision 双栏对照中左栏有 BDRC 结果，右栏只显示“未返回”。

## 2026-07-04 第27次对话提示词

- `14:43` AS译林本地副本直译报错，用户提供三个界面截图：顶部状态分别显示“直译生成 · 1 运行中”“直译生成 · 1 失败”“直译生成 · 1 运行中 · 1 失败”，要求检查直译链路。

## 2026-07-04 第28次对话提示词

- `15:24` 继续处理藏文 OCR 工作台右栏 AI Vision 无输出问题，要求沿当前链路继续修正并验证。

## 2026-07-04 第29次对话提示词

- `15:32` “Gemini 上游先是 429 配额耗尽”这个问题，用户说明已经提供至少 5 个 API key，要求设置轮值：用完一个，再调用另一个 API Key。

## 2026-07-04 第30次对话提示词

- `16:17` 用户指出 BDRC 与 AI Vision 仍然是在同一个 OCR 结果面板内拆成两部分，要求改为页面级独立两栏。

## 2026-07-04 第31次对话提示词

- `16:34` 用户询问 AI Vision 使用什么模型，以及为什么右栏后面几行没有显示；截图显示 AI Vision 只返回前 4 行，后续行显示“未返回”。

## 2026-07-04 第32次对话提示词

- `17:12` 用户反馈 AI Vision 调用失败时提示无法连接 `http://127.0.0.1:18092/ocr`，但错误文案误导用户启动 BDRC OCR 服务。

## 2026-07-04 第32次对话提示词

- `16:47` 点击 AS译林右栏 `AI 翻译` 报错：`翻译请求失败: LLM error for '钦哲仁波切': LLM gateway error 502 Bad Gateway`，错误详情显示远程 `1cnumodel.cpolar.top` 超时且 `/v1/chat/completions` 路径返回 404，要求继续打通本地 AI 翻译调用链路。

## 2026-07-04 第33次对话提示词

- `19:51` 用户撤回上一条 AI Vision / `18092` 报错，说明发错项目，要求继续处理本地 AS译林副本。
- 继续修正 AS译林本地副本中可编辑 PDF 的直读文本优先链路：右栏不应再读取旧 `pdf_page / tesseract` OCR 结果，而应使用 `text / text_import` 结果。

## 2026-07-04 第34次对话提示词

- `22:03` 用户要求“把第一栏也按对应中间和右边栏的 block 作同步框”，即 AS译林本地前端的原文栏也应按段落 block 展示，并与右侧译文 block 对齐。

## 2026-07-04 第34次对话提示词

- `21:12` 用户要求去掉藏文 OCR 工作台最右边的空白，继续优化三栏对照布局宽度。

## 2026-07-04 第35次对话提示词

- `21:33` 用户再次截图指出 AI Vision 右侧仍有空白，要求把三栏撑满；继续修正折叠翻译栏占位与浏览器缓存问题。

## 2026-07-04 第36次对话提示词

- `21:41` 用户再次反馈右侧依然是空白；要求继续去掉右边栏右侧空白，把三栏撑满。

## 2026-07-04 第37次对话提示词

- `21:49` 用户截图指出第4个边栏依然被占用；要求继续清除 AI Vision 右侧的隐藏栏占位，让页面只保留并撑满原文、BDRC OCR、AI Vision OCR 三栏。

## 2026-07-04 第38次对话提示词

- `22:08` 用户截图指出左侧页面导航仍单独占用一列，要求将页面导航放到左栏上方，以节省横向空间。

## 2026-07-04 第39次对话提示词

- `22:31` 用户截图指出页面导航应继续上移到原文栏顶部的 `1 / 5` 页码旁边，而不是放在加载文件按钮下方。

## 2026-07-05 第40次对话提示词

- `00:24` 用户要求：1. 左栏宽度可以调整；2. 每个边栏都可以折叠；3. BDRC OCR 和 AI Vision 识别不同的字符需要高亮；随后补充左栏页面导航不需要预览，直接按页码导航即可。

## 2026-07-04 第38次对话提示词

- `22:14` 用户要求把第二类任务重点整理文档集中到一个文件夹，不要继续散落在项目根目录；同时保留原命名规则和单文档归档约束。

## 2026-07-04 第40次对话提示词

- `22:46` 用户截图反馈藏文 OCR 工作台中“删除”和“新建项目”没有响应，并在后续要求继续处理。

## 2026-07-04 第41次对话提示词

- `23:36` 用户截图反馈 AS译林本地副本 `http://127.0.0.1:18088/?_sutra_local_v=8-block-sync-final` 页面响应特别慢，并出现浏览器“1 个页面无响应”提示。

## 2026-07-05 第42次对话提示词

- `01:54` 用户要求：把第一栏也按对应中间和右边栏的 block 做同步框，使原文栏能按 BDRC OCR 与 AI Vision OCR 的 block/line 对照关系显示同步定位框。

## 2026-07-05 第43次对话提示词

- `19:20` 用户截图反馈 AS译林本地副本当前 PDF 页面右栏仍显示之前文档的 OCR 识别结果，要求清除旧 OCR 残留。

## 2026-07-06 第44次对话提示词

- `00:19` 用户截图反馈藏文 OCR 工作台：左栏和中栏顶部需要对齐，去掉左栏 `ocr 识别进行中` 状态，改为在同一区域显示 `加载文件`；页面导航需要从页码圆点形式改为首页、上一页、页码输入、总页数、下一页、末页的紧凑控件；随后要求继续完成。

## 2026-07-06 第45次对话提示词

- `13:45` 用户截图指出藏文 OCR 工作台顶部的识别引擎、BDRC OCR 接口、AI Vision OCR 接口、OCR 图片 DPI、页码、显示比例等信息没有必要保留，要求继续清理主界面冗余信息。

## 2026-07-06 第46次对话提示词

- `16:35` 用户截图指出正常载入提示横条和顶栏副标题也没有必要保留，要求继续精简藏文 OCR 工作台首屏信息密度。

## 2026-07-06 第47次对话提示词

- `17:38` 用户要求在藏文 OCR 工作台增加另一种对照样式：左栏保留原文，中栏和右栏合并为校对区，按 block 逐组显示原文 block、该 block 的 BDRC 识别结果、该 block 的 AI Vision 识别结果，并允许修改与保存选定结果。

## 2026-07-06 第48次对话提示词

- `17:53` 用户通过截图反馈新校对视图仍有显示问题：右侧 block 被压成细条，左侧原文页显示大量同步框，影响阅读和校对。

## 2026-07-06 第49次对话提示词

- `18:03` 用户截图反馈校对 block 看起来只显示了 BDRC 识别结果，没有显示 AI Vision 识别结果，要求修正校对视图中两种识别结果的展示。

## 2026-07-06 第50次对话提示词

- `21:08` 用户截图反馈 BDRC 识别和 AI Vision 识别并排后横向空间过窄，要求再次尝试不要分两栏，而是在同一 block 内分为上下两行显示。

## 2026-07-06 第51次对话提示词

- `21:21` 用户再次澄清校对视图的真实需求：仍按原文 block -> BDRC 识别 -> AI Vision 识别的顺序逐 block 展示，BDRC 和 AI Vision 都可编辑，每个 block 可选择采用哪一版并点击保存修改写回当前页 OCR 结果。

## 2026-07-06 第52次对话提示词

- `22:00` 用户截图反馈：1. 校对视图没有显示 AI Vision OCR 识别结果；2. 原文 block 图片没有根据文本框大小自适应；3. OCR 识别结果需要保留此前对含上下加字藏文字母的红框标识。

## 2026-07-07 第53次对话提示词

- `00:02` 用户要求将本地 AS译林 `http://127.0.0.1:18088/` 设置为无需账号即可登录。

## 2026-07-07 第54次对话提示词

- `16:08` 用户截图反馈藏文 OCR 校对视图依然没有显示 AI Vision 识别结果，并要求继续修正。

## 2026-07-07 第55次对话提示词

- `16:14` 用户截图反馈 AS译林本地副本中 P2 点击执行 OCR 后右栏识别结果仍是藏文乱码，强调该书是文字可编辑 PDF，不应执行 OCR，应直接提取文本。

## 2026-07-07 第56次对话提示词

- `16:30` 用户截图反馈藏文 OCR 校对视图多轮修改后仍未改成指定样式，要求严格按“原文 block 预览 -> BDRC block 预览 -> AI Vision block 预览”的三段式结构展示。

## 2026-07-07 第57次对话提示词

- `16:33` 用户截图反馈 AS译林本地副本点击 `AI 翻译` 报错：`LLM gateway error 502 Bad Gateway`，详情显示 `/api/aggregate/chat -> ConnectError: All connection attempts failed`。

## 2026-07-07 第58次对话提示词

- `16:49` 用户截图反馈藏文 OCR 校对视图中 `AI Vision block` 仍然没有显示 LLM 的 AI Vision 识别结果，要求继续修正。

## 2026-07-07 第59次对话提示词

- `20:28` 用户截图反馈依然没有看到 `AI Vision block 03` 的识别预览，并询问为什么反复修改仍未解决。

## 2026-07-07 第60次对话提示词

- `19:55` 用户截图反馈 AS译林本地副本点击 `学术直译` 后没有返回可用翻译结果，界面显示 `学术直译 · AI 建议（中文） · 未入流程`，需要定位为什么中译英流程仍停在中文建议。

## 2026-07-07 第61次对话提示词

- `21:02` 用户截图反馈藏文 OCR 校对视图里 AI Vision block 仍然只显示标题、不显示识别预览；同时原文 block 图片超过预览窗口，需要适当放大预览窗口。

## 2026-07-07 第62次对话提示词

- `20:56` 用户截图反馈 AS译林本地副本文本型 PDF 的原文 block 按视觉行切分，导致句子不完整；询问是否应以自然段为 block，并要求修改。

## 2026-07-07 第63次对话提示词

- `21:17` 用户截图反馈藏文 OCR 工作台中模型聚合依然没有发挥作用，要求参考 AS译林本地项目 `http://127.0.0.1:18088/` 如何调用 AI Vision。
- `22:00` 用户截图反馈校对视图中原文 block 预览没有显示，AI Vision block 识别结果依然没有显示，要求继续修正。

## 2026-07-07 第64次对话提示词

- `22:46` 用户截图反馈藏文 OCR 校对视图右栏仍出现一串 `原文 block xx 预览`，要求调整右栏展示，使右栏不再被原文预览占用。

## 2026-07-07 第65次对话提示词

- `23:03` 用户截图反馈藏文 OCR 校对视图右栏仍然看不到 BDRC / AI Vision 正文，只显示 `BDRC block xx 识别` 标题，要求继续修正右栏显示。

## 2026-07-07 第64次对话提示词

- `22:13` 用户截图反馈 AS译林本地副本中当前文档是文本可编辑 PDF，不需要 OCR，但页面和左侧导航仍显示 `pending`，要求解释并修正状态与直读流程。

## 2026-07-07 第66次对话提示词

- `23:19` 用户截图反馈藏文 OCR 校对 / Mathpix 校正文本中夹杂多余中文空格，要求去掉文字中的空格。

## 2026-07-08 第67次对话提示词

- `00:36` 用户截图反馈 AS译林本地副本点击 `重新直读 / 全部直读` 后确认弹窗仍显示 `跑 OCR`、`每页一次 LLM Vision 调用`，要求处理误导文案。

## 2026-07-08 第68次对话提示词

- `11:22` 用户截图反馈藏文 OCR 工作台在端口地址 `127.0.0.1:8790/tibetan-proofreading-app/?v=20260708-proofread-blocks-19` 中，校对视图右栏仍然只显示 `原文 block xx 预览` 标题，不显示 BDRC / AI Vision 正文，要求彻底修正右栏显示问题。

## 2026-07-08 第69次对话提示词

- `12:11` 用户截图反馈校对视图右栏已正常显示，但每个 block 最上面的原文预览范围过大，只需要显示左栏同步框对应的原文内容。

## 2026-07-08 第69次对话提示词

- `11:58` 用户反馈 AS译林本地副本点击“全部直读”按钮后，右栏没有显示任何结构，要求继续打通可编辑 PDF 的直读结构显示链路。

## 2026-07-08 第70次对话提示词

- `15:51` 用户反馈 AS译林本地副本点击“重新直读”和“全部直读”没有任何响应，要求定位并修复直读按钮链路。

## 2026-07-08 第71次对话提示词

- `15:48` 用户截图反馈藏文 OCR 校对视图的原文预览仍偏小，要求适当放大，同时只显示同步框里的文字。

## 2026-07-08 第72次对话提示词

- `16:52` 用户截图反馈藏文 OCR 校对视图中原文预览可以适当缩小，并要求 `BDRC block 识别` 与 `AI Vision block 识别` 的字体做成可以缩放的样式。

## 2026-07-08 第73次对话提示词

- `16:55` 用户截图反馈 AS译林本地副本点击“全部直读”和“重新直读”已有反馈，但右栏依然没有显示文本，要求继续定位直读后展示链路。

## 2026-07-08 第74次对话提示词

- `17:22` 用户截图反馈 AS译林本地副本中右栏虽然显示部分分段，但下面段落没有显示，部分 segment 显示 `0 tokens`，要求继续修复直读后 token 刷新链路。

## 2026-07-08 第74次对话提示词

- `17:23` 用户截图反馈藏文 OCR 校对视图在页面宽度缩小时，原文预览不会像 BDRC / AI Vision block 识别结果一样自动缩小，要求修正原文预览响应式缩放。

## 2026-07-09 第75次对话提示词

- `11:29` 用户要求按建议配置 AS译林本地副本的聚合器 API，使角色管理中的 gv-reseller 兼容入口继续指向本地适配器，并由适配器转发到自己的 ModelAggregatorService。

## 2026-07-10 第76次对话提示词

- `13:24` 用户截图反馈藏文 OCR 校对视图中需要新增功能，可以将 BDRC 和 AI Vision 都识别错误的字母进行标记。

## 2026-07-10 第77次对话提示词

- `15:23` 用户要求给藏文 OCR 校对视图中的原文 block 预览也增加放大 / 缩小按钮。

## 2026-07-10 第78次对话提示词

- `15:42` 用户截图反馈校对 block 底部操作按钮太多，询问是否可以凝练。

## 2026-07-10 第79次对话提示词

- `16:04` 用户截图反馈校对 block 操作区需要把“同错”改为“错误”，并让“保存”按钮字体和前两个按钮保持同样大小。

## 2026-07-10 第80次对话提示词

- `16:03` 用户截图反馈 AS译林本地副本界面整体按钮偏多，要求适当凝练；同时询问段落卡片里的 `Seg #3 生` 和 `tokens` 的作用，以及是否有必要保留。

## 2026-07-10 第81次对话提示词

- `16:21` 用户要求修改 AS译林本地副本右栏：中文源文下不应重复显示“等待中文译笔”和“添加译文”，并需要将“原文已校对”的含义改得更清楚。

## 2026-07-10 第82次对话提示词

- `16:28` 用户截图反馈藏文 OCR 校对 block 操作区中“错误”按钮文案需要改为“标错”。

## 2026-07-10 第83次对话提示词

- `16:49` 用户截图反馈 AS译林本地副本点击“等待中文译笔 — 点击此段开始翻译”后进入新译文编辑框，但不能收起；要求支持只是查看时能退出编辑态。
- `21:18` 用户继续反馈当前仍不能收起；要求继续修正该编辑态退出问题。

## 2026-07-10 第84次对话提示词

- `20:58` 用户截图反馈藏文 OCR 校对 block 操作区中按钮图标和字体没有居中对齐，要求统一水平、垂直居中。

## 2026-07-10 第85次对话提示词

- `23:14` 用户截图反馈 AS译林本地副本右栏新译文编辑器中的“展开/收起”按钮位置不合适，要求放到最上边“撤回”按钮的左边。

## 2026-07-11 第86次对话提示词

- `12:53` 用户要求在 `http://127.0.0.1:8790/tibetan-proofreading-app/` 增加主页，包含“藏文 OCR 识别和校对”与“藏译中”两个入口；点击每个入口后可以新建项目、浏览当前项目、继续正在校对或正在翻译的任务。

## 2026-07-11 第87次对话提示词

- `15:39` 用户截图反馈首页中“藏文 OCR 识别和校对”和“藏译中”两个入口的“新建项目”都会跳转到 OCR 校对页面，要求修复入口分流。

## 2026-07-11 第86次对话提示词

- `13:55` 用户要求继续处理洞见项目 AS译林子项目中部分模型无法调用的问题，按“先验证聚合器直调，再修适配器真实模型映射，最后接回 AS译林”的顺序定位和修复；重点核查 `qwen3.6-27b:latest`、`gemma2:27b`、`batiai/qwen3.6-27b:q4`，避免把多个模型名称伪装成 `qwen2.5:14b`。
- `14:20` 用户补充运行态检查结果：Qwen2.5、Gemma2、Llama3、Gemma4 已可用，但两条 Qwen3.6 仍在聚合器直调中 90 秒超时，要求继续定位聚合器根因，修通后再恢复适配器和 AS译林角色通道。

## 2026-07-11 第87次对话提示词

- `15:25` 用户要求重新核验模型聚合服务宣传的“其他模型直调已修复”是否属实，并继续把已修复的真实模型链路接回 `as-yilin-model-adapter` 与本地 AS译林 副本。

## 2026-07-11 第88次对话提示词

- `16:12` 用户要求增加一个功能：让聚合器里的不同模型对同一段内容用所有可用模型同时翻译，并在 AS译林 本地链路里直接进行结果对比。

## 2026-07-11 第89次对话提示词

- `19:01` 用户希望在 AS译林 本地副本的角色管理里精简角色，接受“隐藏/停用内置角色”的方案，以减少 `内置` 角色对界面的干扰。

## 2026-07-11 第90次对话提示词

- `22:10` 用户补充要求：不仅角色管理弹窗里要隐藏旧的内置角色，右栏新译文编辑区中的重复内置角色按钮也要同步隐藏。

## 2026-07-11 第90次对话提示词

- `21:50` 用户要求添加 `gemini-3.1-flash-lite`，并将其作为适合翻译的可调用模型接入。

## 2026-07-12 第91次对话提示词

- `11:16` 用户反馈首页两个入口的“新建项目”仍然都会跳转到 `http://127.0.0.1:8790/tibetan-proofreading-app/`，要求继续修复，让 OCR 和藏译中入口进入不同的工作流地址与页面状态。

## 2026-07-12 第92次对话提示词

- `11:40` 用户截图反馈点击“新建项目”依然跳转到校对页面。经检查右侧藏译中入口已进入 `workflow=translation`，但两个入口按钮同名造成判断混淆，因此需要把首页入口按钮文案改为“新建 OCR 项目”和“新建翻译项目”。

## 2026-07-12 第93次对话提示词

- `12:16` 用户确认需要优化多项目处理：新建项目不应承担选择已有项目的职责；如果已有多个项目，继续任务时应进入项目列表选择，而不是直接打开某一个项目。

## 2026-07-12 第91次对话提示词

- `11:10` 用户反馈 AS译林 本地副本右栏角色按钮“这一排还是没变”，即使强制刷新后仍未隐藏重复的旧内置角色，要求继续修正前端显示逻辑。

## 2026-07-12 第94次对话提示词

- `12:58` 用户截图反馈 AS译林 本地副本中“译师会审”只返回两个角色译文，同时没有看到聚合器多模型翻译的对照结果，要求修复角色会审完整性并提供可见的多模型对照入口。

## 2026-07-12 第95次对话提示词

- `15:21` 用户截图反馈当前“多模型对照”仍以纵向纯文本显示，要求参考“译师会审”的输出方式，改成并列对照、分歧高亮和可采纳结果。

## 2026-07-13 第96次对话提示词

- `12:36` 用户询问 AS译林翻译多模型中哪个最适合中翻英任务，涉及 `agg-local-qwen25`、`agg-local-qwen36`、`agg-local-gemma2`、`agg-local-qwen36-q4`、`agg-compare-all`、`agg-gemini-25-flash` 和 `agg-gemini-31-flash-lite`。
- `12:36` 用户要求模型名称更精确，例如使用 `2.5` 而不是 `25`。
- `12:36` 用户确认按建议修改配置。

## 2026-07-13 第97次对话提示词

- `11:26` 用户截图反馈文本型 PDF 直读后，左栏章节标题“第三章：物理学被锁死百年”和下一行“光速不可超越吗？”在右栏被黏连成同一段，要求修复标题与正文首句的分段。

## 2026-07-13 第98次对话提示词

- `17:03` 用户要求根据本项目文件夹内容撰写“宇宙模型智能体研究规划”，纳入之前的问答功能、模型聚合器和 OCR 校对等功能；将“数学、天文书籍的 OCR 校对（包括 Mathpix API 调用 + Codex 开发）”列为正在进行的计划，并预估研究时间、开发费用、Token 费用、Zeabur 轻量部署费用，以及云服务器 OSS 和 RDS 超过 3 个月免费试用期后的费用。
- 用户随后两次回复“继续”，确认按 Preflight Plan 直接完成文档撰写和项目归档。

## 2026-07-13 第98次对话提示词

- `18:07` 用户将“洞见 / AS译林本地副本”续接到新线程，要求先遵守根目录 `AGENTS.md`，只完成批量直读空白页提示修复的收尾：只读核对既有 `Dockerfile.web-login-patch` 变更与脏工作区，检查 Compose、首页和容器内 v33 前端资源，浏览器点击“全部直读”确认 P563、P564 被分类为没有文本层而非请求失败，检查控制台，补齐提示词历史和且仅一个任务摘要，并运行针对性 Git 检查；不得混改藏文 OCR/翻译工作台、线上应用或暴露密钥，无法隔离既有改动时不得自动提交推送。

## 2026-07-13 第99次对话提示词

- `18:20` 用户认为 AS译林本地副本右栏 block 不应逐句切分，而应把一个完整自然段作为一个 block，让翻译模型获得更多上下文。

## 2026-07-13 第100次对话提示词

- `19:01` 用户要求参考 Codex 任务 `019f5aab-7f04-73f1-a047-5aabb2f7ede4` 撰写 AS译林研究规划。参考任务确定的口径包括：研究周期 6 个月、志愿者协作不计人工费用、Codex 开发套餐和 Zeabur 服务器与仅腊项目共享并只计算增量成本。

## 2026-07-13 第101次对话提示词

- 用户补充 AS译林的主要用途：将《自在沉思录》丛书由中文翻译为英文；对藏文典籍进行 BDRC OCR 与 LLM 直接识别的对照；支持藏译中。现有模型资源包括自有服务器千问系列、多个免费层级 Gemini API key 号池及云雾 API，要求将这些信息补充到研究规划。

## 2026-07-13 第102次对话提示词

- `20:52` 用户要求在《AS译林研究规划》中增加对 BDRC 软件的简要介绍。

## 2026-07-13 第103次对话提示词

- `20:59` 用户要求在《AS译林研究规划》中感谢陈刚老师为团队所做的前期开发，并明确当前中译英界面主要基于陈刚老师前期开发的应用；本项目主要通过接入自有服务器千问开源模型、Gemini 免费层级 API key 号池和云雾 API 等方式进一步压缩 LLM 调用费用。

## 2026-07-13 第103次对话提示词

- 用户要求参考 Codex 任务 `019f0ec4-9749-7482-a199-018d2897ce29`，把藏文 OCR 对照识别与藏译中工作台部署到 Zeabur，并将待校对书籍统一放入一个新建的阿里云 OSS Bucket。

## 2026-07-13 第104次对话提示词

- `20:50` 用户截图反馈 AS译林本地副本文本型 PDF 页面末尾若干段落在左栏可见，但右栏段落卡片没有显示原文，询问为何没有识别出来。
- `21:02` 用户确认直接修改。经只读核对，末尾三段已存在于数据库 `source_tokens`，因此要求只修复本地前端原文显示，不重新 OCR、不重分段、不覆盖数据库或既有译文。

## 2026-07-13 第105次对话提示词

- 用户指定复用 Codex 任务 `019f0ec4-9749-7482-a199-018d2897ce29` 对应项目相同的 Zeabur 控制台登录态和阿里云登录凭证，并提供参考路径 `/Users/Min369/Documents/同步空间/Manju/AIProjects/UnivModel-v1.0/宇宙模型MVP/`。
## 2026-07-16 第106次对话提示词

- 用户要求先将洞见工作区的项目代码安全同步到 GitHub 仓库；排除大型资料、真实密钥和无关本地文档。
- 用户确认按完整方案实现 AS译林跨页逻辑段落：新增跨页关联状态；前端显示“跨页续段候选”和“确认合并”；确认后不删除两页原文；翻译时作为一个完整段落；支持撤销合并。

## 2026-07-16 第107次对话提示词

- `22:35` 用户回复“继续”，要求完成上一轮藏译中工作台布局修复：翻译右栏应与左栏保持一致的面板样式，正文横向铺满，不再挤压成最右侧窄条。

## 2026-07-17 第108次对话提示词

- `16:01` 用户截图反馈 OCR 校对视图左栏宽度不可调整，要求恢复原文栏与 OCR 校对结果栏之间的拖动调宽功能。

## 2026-07-17 第108次对话提示词

- `11:08–11:14` 用户提供五张参考截图，要求将 AS译林本地副本左侧逐页长列表改为紧凑的首页、上一页、页码输入、总页数、下一页、末页导航；左侧栏支持页面缩略图和目录两种视图；同时精简原典顶部过多的操作按钮。

## 2026-07-17 第109次对话提示词

- `12:02–12:03` 用户提供四张界面截图，要求去掉底栏“藏→中、 中→英”进度文字；将页面导航从最左侧目录栏移动到左栏原文上方；放大“缩略图 / 目录”图标并去掉文字；同时询问“未分章”的用途。

## 2026-07-17 第110次对话提示词

- `12:27` 用户截图反馈左侧 PDF 页面上方字体和控件占用空间过大，要求将原典标题工具、页面导航、PDF 工具三行压缩为一行，并去掉左上角与右侧重复的字号放大控制。

## 2026-07-17 第111次对话提示词

- `13:31` 用户截图要求去掉界面中的“未分章”和“原典”，并将页码导航合并到顶部项目与当前页所在的同一行。

## 2026-07-17 第112次对话提示词

- `15:39` 用户提供两张截图，反馈右栏鼠标从段落原文移动到下方译文编辑框时，译者标签会移动，要求修复标签区域的布局稳定性。

## 2026-07-17 第113次对话提示词

- `15:40` 用户截图要求左侧缩略图浏览框宽度可拖动调整，其中的 PDF 页面缩略图随容器大小自适应。
- `15:41` 用户补充截图，要求点击左栏或右栏相应原文时，两栏能够双向同步定位和高亮。

## 2026-07-17 第114次对话提示词

- `16:21` 用户截图指出“等待 中文 译笔—点击此段开始翻译”和“添加译文”点击后进入同一位置，询问并要求将两个重复入口合并为一个。

## 2026-07-17 第115次对话提示词

- `17:07–17:10` 用户提供两张截图，反馈统一后的“添加中文译文”入口字体过大，并要求参考 OCR 校对工作台的左右栏样式，整理当前较乱的布局和视觉层级。

## 2026-07-17 第116次对话提示词

- `17:24` 用户截图反馈左右同步定位后出现的蓝色同步框过于夸张、尺寸过大，要求收敛同步高亮样式。

## 2026-07-17 第119次对话提示词

- `17:51` 用户提供两张截图，反馈同步框没有找准左侧 PDF 中对应段落的位置，要求修复段落定位准确性。

## 2026-07-17 第120次对话提示词

- `16:15` 用户截图反馈藏文 OCR 工作台的 AI Vision 结果错误显示为 `mathpix:mathpix-text`，指出 Mathpix 适合数学公式识别、不适合藏文，要求修正 AI Vision 模型路由。

## 2026-07-17 第121次对话提示词

- 用户要求新增将藏文 OCR 识别与人工校对后的最终结果保存为 Markdown 文档，并支持导出或下载。

## 2026-07-17 第122次对话提示词

- `18:03–18:04` 用户提供截图反馈 AS译林本地副本在浏览器调整大小时，页码导航会从左侧原文区域挤到全局顶部工具栏；要求页码导航固定在左侧原文栏。同时反馈点击“通知”按钮后的通知面板超出主屏幕宽度，要求限制在可视区域内。
